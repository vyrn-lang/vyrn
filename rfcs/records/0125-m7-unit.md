#### A Unit value is no value, so its `let` and its store emit only their effects (2026-09-25, `m7-unit`)
RFC-0125, milestone M7.
Decision: the core walk gives a Unit name no place. A read of it writes nothing, and a `let` or store of it emits only the right-hand side's effects, as the arm's statement does. `Fn_::core_unit` states this once, and five sites ask it: the names clause, `core_val_readable`, the store screen, `core_val`, and the `let` and store emission. The lead's rule (brief `m7-unit`), built on `m7-join` (#455), which left the 22-body Unit join.
Went: nothing; the walk had no reader for a Unit value. Stayed: a Unit field, an array of Unit and a Unit parameter, which the arm refuses too.
Lines: `direct.rs` 21,361 to 21,387 (+26: the helper and its doc 8, the store arm 8, and one line or a block at each other site). Refusals: 0 lost / 0 gained. Manifest: 7 rows, below.
Licence:
- measure-walk instrument, applied uncommitted, `8e848183` against the rule: taken 20,684 to 20,695 of 21,176, refused 492 to 481. 24 bodies met a Unit-name or Unit-store clause before and 0 after. 11 are taken; 13 are still refused for another clause (below). The taken/refused multiset moved in these 11 alone.
- `coredrive --ignored` at the tip: taken 20,695 of 21,176, 1 byte-identical, 167 run the same, 0 run apart; the new shape at 0 break, 0 continue.
- the 7 moved rows, built with `VYRN_WASM_NAMES=1` by `e3e6023a`'s binary and the tip's; `wasm2wat` differs in the 11 bodies only: `blockarms` `main`; `enumarray` `describe`, `showLoad`, `showOpt`; `fnvalstore` `showValidation`, `showResult`; `graphql` `ask`, `main`; `listdir` `show`; `nestedsum` `show`; `simdmem2` `main`. The rows read a scrutinee in place and build a payload in its slot, where the arm copied. `memory.copy` fell in 7 functions (`describe` 7 to 3, `showValidation` 4 to 2, `graphql` `main` 6 to 4). Frames shrank in 7 (`describe` 128 to 32 bytes, `simdmem2` `main` 144 to 64, `graphql` `main` 208 to 176, `listdir` `show` 48 to 16, `showValidation` 64 to 16, `showResult` 32 to 16, `blockarms` `main` 48 to 32). Each `local.tee` became `local.set` and `local.get`. `graphql` `main` holds 8 more `drop`s: the two drops of an in-place build at 4 sites, where the arm used a `memory.copy`. Not measured for time.
- the shape `a-unit-join-of-a-match-statement`: two `match` statements whose arms call `print` and a Unit function. `show` and `vyrnTestMain` emit different wasm from `e3e6023a`'s binary. Both binaries print the same 5 lines with the core walk on and off, under `VYRN_LEAK_CHECK=1`.
- the 7 moved programs under `VYRN_LEAK_CHECK=1`, `e3e6023a`'s binary and the tip's: the same output, exit 0, no leak line.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `kernel` 27,066 / 0 / 0. `VYRN_WASM_MANIFEST=check` green after `write`.
- `vyrn check` over the corpus roots, `e3e6023a`'s binary against the tip's: the 481 shared roots equal; 3 shapes gained (`m7-falias`', `m7-join`'s and this one), all accepted.
- pins by `VYRN_PIN=write`: `emitter-census` the mapping 11,782 to 11,808; `emitter-reads` both, for two questions 9,939 to 9,965 lines and 301 to 302 rows; `surface` `Type::Unit` wasm 32 to 33, all 1,432 to 1,433.
Findings:
- the census row counted 22 Unit-join bodies. This instrument counts 24 that meet a Unit name: 19 with a Unit join temporary, and 5 (`capturefn` `shiftAll`, `zipApply`, `viaOnward`; `lambdas` `main`, `sum`) whose only Unit name is a closure of type `Fn(..) -> Unit`. Those 5 stay refused by the lambda clause. The 11 taken are all join bodies.
Left: the other 13 bodies. 7 are blocked by a lambda (`capturefn` `shiftAll`, `zipApply`, `viaOnward`; `fnvalarg`, `fnvalstore` and `lambdas` `main`; `lambdas` `sum`). 5 are blocked by an enum `@borrow` read out of a map key (`fnvalstore` `fireInt`, `fireUser`, `fireVal`, `fireRes`; `nestedsum` `main`). 1 is blocked by a Map name bound by `@tally` (`wirekey` `main`).
