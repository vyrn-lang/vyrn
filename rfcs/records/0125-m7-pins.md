# A pin is data a gate writes, so two tracks never conflict on it (2026-09-24, `m7-pins`)
RFC-0125, milestone M7.
Decision: move every pin the M7 merges fought over into files a gate writes, and let git keep either side of one; the lead's brief, agreed by the user on 2026-09-24.
The count, over the last ten merged M7 branches (#428 to #442): the emitter census's two `want` tables 8 of 10 each; `SHAPES` with `SHAPE_PIN` 6; `wasm-sha256.tsv` 6; RFC-0126's cost table and total 5; RFC-0127's form table and total 4; `frontend_census` 2; `checker_census` 1. The appended RFC-0125 records (4) left with `m7-records`; `lowered.rs`'s bound (1) and `core-gaps.md` (4) are not pins and stayed.
Went: the nine census tables into `compiler/vyrn-cli/tests/pins/*.tsv` (`emitter-census`, `emitter-reads`, `frontend-census`, `checker-census`, `surface`, `forms`, `declarations`, `keywords`, `contextual`), 170 lines; `SHAPES` and `SHAPE_PIN` into 57 files under `tests/shapes/`, 299 lines, each with `// <description>` and `// pin: <b> break, <c> continue` as its first lines and its comment after; RFC-0126's cost table and RFC-0127's four tables out of the prose, which names the file; the two ignored printers `the_surface_census_as_a_table` and `the_form_census_as_a_table`, which `VYRN_PIN=write` replaces. Stayed: `PIN` and `CALLS` in `coredrive.rs`, unmoved in all ten merges; the verdict tables, which a person writes.
Lines: `coredrive.rs` 990 to 559, `forms.rs` 1,162 to 980, `surface.rs` 351 to 293, `frontend_census.rs` 1,118 to 1,077, `emitter_census.rs` 947 to 942, `checker_census.rs` 775 to 772, `common/mod.rs` 553 to 595 (`pin`, `pin_write`, `pin_row`); test Rust 5,896 to 5,218. RFC-0126 1,583 to 1,548, RFC-0127 690 to 596, `.gitattributes` 22 to 29. Refusals: 0 lost / 0 gained. Manifest: untouched.
Licence:
- every pin file written by `VYRN_PIN=write` from the old tree's code equals the table it replaced: the five RFC tables cell for cell against `git show HEAD`, totals 1,431 / 861 / 243 / 122 / 45 equal to the prose they replace; the four census tables line for line.
- the 57 shape texts, read back by the new reader's rule, are byte-identical to a dump of `SHAPES` taken by a temporary test in the old tree.
- `coredrive --ignored` before and after: every line equal, the shape lines compared sorted (file-name order replaced declaration order); 168 programs, 21,146 bodies, taken 20,418 of 21,176, 1,647 carried end to end, 1 byte-identical, 167 run the same, 0 run apart, 57 shapes at 0 break, 0 continue.
- the write mode: one number corrupted in each of the nine pin files and one shape's pin; without `VYRN_PIN` ten gates failed, each naming its file and line; with `VYRN_PIN=write` all passed and `git diff` was empty.
- the merge driver, in a scratch repository with this `.gitattributes`: two branches changed the same pin line and the same manifest row. Without `merge.pin.driver` the rebase stopped on both as before; with `git config merge.pin.driver true` it finished and kept the upstream side, and the branch's code change landed. A pin changed on one side only merged to that side.
- `vyrn check` over the corpus roots, main's binary (`8d93019d`) against the tip's: the 415 shared roots equal; 57 roots gained, the shape files, all accepted (339 to 396 accepted, 76 refused).
- `kernel` 27,066 / 0 / 0; `effects` 29,624 judged, 0 unattributed; `typed` 236,919, 0 unjudged; `coretables` green; `VYRN_WASM_MANIFEST=check` green over 174 examples. `cargo nextest run --release -p vyrn-cli` 666 passed, 0 failed, 45 skipped (47 before: the two printers went). Both formatters clean; the release build has no warning.
Time: about 90 minutes: 50 work, 35 gates, 5 waiting on builds.
Findings:
- `scripts/check-corpus.sh`, `lowered_dump.rs` and `forms.rs`'s corpus walk read every `.vyrn` under `compiler/vyrn-cli/tests/`, so the shapes are corpus roots now: `vyrn check` accepts each without its `main` wrapper.
- `merge=pin` keeps the current side, which in a rebase is main's and in a merge is the target's. A merge of a branch that is not rebased can leave a stale pin on the target; the gate on the target fails, and the write mode fixes it.
Left: the lead sets `git config merge.pin.driver true` in the shared repository config after this merges; without it a pin conflicts as text, as before.
