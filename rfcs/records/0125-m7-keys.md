#### A body the core built is found by the name it was built under (2026-09-25, `m7-keys`)
RFC-0125, milestone M7.
Decision: `vyrn_lower::spell` is the one home of an instance's key and `core::lambda_spelling` of a lambda's; the emitter enqueues each body with the key the core built it under and asks `body_of` by it. A key two bodies share names neither. The lead's brief `m7-keys`, from the whole-body census of `m7-names3`.
Went: the emitter's bare-name lookup; `Fn_::core_body`'s parameter-count error, which the lookup now states as a screen. Stayed: the 13 `Owned__Slots__release<T>` bodies, because no core body exists under any key (below).
Lines: `direct.rs` 21,361 to 21,396 (on `35d35669`), `core.rs` 8,815 to 8,827, `lib.rs` 1,488 to 1,495; the key needs a field on `Pending` and on `Fn_`, and the parameter screen is new. Refusals: 0 lost / 0 gained. Manifest: 27 rows, below.
Licence:
- `coredrive --ignored`, main `35d35669` (20,684 of 21,176) against the tip: taken 20,818 of 21,177, plus 74 judged; 1 byte-identical, 167 run the same, 0 run apart. On `5c606532` before the rebase: 20,585 to 20,718. `polyrecursion.vyrn` emits 133 bodies, one more than on main; not traced.
- the census row "no core body", with the measure-walk instrument applied and not committed, on `5c606532`: 242 bodies before, all refused at the lookup (170 generic instances, 59 lambdas, 13 `Owned__Slots__release`). After, under the spelled keys, 243 bodies: 133 taken (103 of 184 generic instances, 30 of 59 lambdas), 110 refused. All refused 758 on `e3e6023a`, 591 on `5c606532`, 459 at the tip.
- the 110 a later clause refuses, by first clause: an Array name 33 (`insert<T>`, `remove<T>` of `std/slots`: `s.free`, a field of a `modify` parameter read as a name); parameters differ from the frame's 28 (RFC-0023 specializations `specialize` could not state, `map`, `filter`, `unfold`, `defer`, and `wrapSink@lambda:80`); no instance 13; a Stream name 12 (stream lambdas); a Unit name 7 (`fnvalstore` lambdas); `let x = call` 6; an Enum name 4; the body holds lambdas 4 (`take<Int64>`); two bodies share the key 2 (`closures2`'s line 88); a Record name 1 (`sortWith<Entry>`).
- 10 bodies outside the row moved from a later clause to the parameter screen: RFC-0023 specializations whose declaration body kept its `fn` parameter (`capturefn` `applyAll`, `shiftAll`, `zipApply`, `viaOnward`; `apply` in `closures2` and `fnvalarg`; `lambdas` `firstOf`, `mapThenSum`; `shadowing` `applyTo`). None of the ten functions changed a byte.
- the 26 moved rows, per function from `wasm2wat` with `VYRN_WASM_NAMES=1` against main's binary: every changed function is a generic instance or a lambda. Shapes: operands staged in locals and a short-circuit's result carried in a local where the arm kept a block result (`std/slots` `alive<T>` +20 lines, `insert<T>` +8, `newSlots<T>` +2, in 12 programs); a layout copy the core does not make (`fnvalstore` `force` and `pagesdemo` `runParamQuery` frames 48 to 32 and 64 to 48, `slots` `Copy__Slots__copy` 256 to 160, `copy` `twice` two `memory.copy` fewer, `knucleotide` `sortWith` and `lambdas` `map` one fewer). `insert<T>` reads two fields in another order and `newSlots<T>` calls `takeIdentity` before it computes the store's address; both are loads and a call with no write between them.
- after the rebase onto `35d35669` the rows are 27: the 26 above, with `langbench` and `pagesdemo` at new hashes because main's bytes moved under the same two functions (`weighN`, `runParamQuery`), and `closures2`, whose `@lambda addGuard` is taken now that m7-join places the enum join it was refused for (66 to 82 lines).
- `lowered`'s compared-answers floor: 9,414 answers at the tip, and the floor went from 10,000 to 5,000; only the AST walk answers.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `VYRN_WASM_MANIFEST=check` green at the first commit on `e3e6023a` (`d28791e4`, before the rebase), which moves no row.
- three shapes: a generic instance and a lambda, each emitting a different module from main's binary with the same output; two lambdas on one line.
Findings:
- the core keyed two lambdas on one line under one name, and the second replaced the first. With the lookup fixed and no clash rule, `closures2.vyrn` printed -198 where 2 is right: `Ops { plus: x -> x + 100, minus: x -> x - 100 }` read `minus` for `plus`. Over the examples, `main@lambda:88` is the one shared key.
- `Owned__Slots__release<T>`: the lowering's worklist queues a generic declared release from `ownership.releases` (`lib.rs`, `dispatched`), and that plan has no step for these bodies; the kernel places the release after the lowering, and the emitter instantiates the body on its own call path. So the core never builds it.
Left: two lambdas on one line, blocked by `Expr::Lambda`, which carries its line and no column; `Owned__Slots__release<T>`, blocked by the lowering's worklist, which does not read the kernel's placed releases; the Array name `s.free`, blocked by the alias rule for a field of a `modify` parameter; the 28 parameter mismatches, blocked by `core::specialize` refusing a bound parameter read another way.
