#### Module state is a place with an address, and a one-operand builtin with no result is one row kind (2026-09-25, `m7-stores`)
RFC-0125, milestone M7.
Decision: the lead's brief (`m7-stores`), from the refused-body census on `513bae71`. Mine: a store into module state is read by the store screen as a store into any place with an address, because the store arm and the key arm already address a global at its static address; `writeStdout` and `close` are one row kind, `Spec::Effect`, and both walks call `Fn_::effect` once the operand is written.
Went: the store screen's `scalar_only` clause, which refused a layout store into a global, a key store into a module-state map, and a String store into a field of module state (9 bodies alone); the AST arms of `writeStdout` and `close`, moved whole into `Fn_::effect` (6 bodies alone). Stayed: `annotates-a-check` (1 body, `finitekeys` `main`), because the core walk states a `where$cTransKey` call at `t("nav.\{about}.label")`, a crossing the checker proved; a cut that admitted a literal under a `where` annotation moved that check into `finitekeys` and `i18ndemo` and was withdrawn. `Store/Elem` (0 alone), the 19 logger bodies of `Do/Call/Builtin`, which meet `Let/Call/Builtin/logger` too.
Lines: `direct.rs` 21,518 to 21,509 to 21,551; `core.rs` 8,910 to 8,915. The row kind costs its variant, two rows, its readable and type arms and a `core_call` arm that writes the operand; the arm's two bodies moved and were not copied. Refusals: 0 lost / 0 gained. Manifest: 4 rows, then 1.
Licence:
- `coredrive --ignored` with `VYRN_WASM_MANIFEST=check`, main `6406139e` against the tip: taken 20,922 to 20,937 of 21,172; carried end to end 1,649 to 1,653; the gap "a callee that is no declared function" 82 to 76; 1 byte-identical, 167 run the same, 0 run apart. With the census instrument on `513bae71` the slices took 20,927 to 20,936 to 20,942, and the moved bodies were the 15 the census names alone.
- the moved rows, `wasm2wat` with `VYRN_WASM_NAMES=1`, main's binary against each slice's. The store, 4: `domdemo` `rotate` and `graphql` `deleteBook` tee the global's address for the snapshot, one line shorter each; `domdemo` `main` and `refutablelet` `ensure` build the new value in a slot of their own, keep the global's old bytes, copy the new bytes in and release the old; `fnvalstore` `onInt`, `onUser`, `onVal` and `onRes` copy the key before they address the map, 156 to 158 lines each. The row kind, 1: `mandelbrot` `main`, whose interpolation temporaries are staged in locals, 377 to 384 lines. The four stream programs emit the same bytes.
- the shape `a-store-into-module-state-of-an-array-an-enum-a-record-and-a-map-key`: an array, an enum, a record, a map key and a String field of module state, stored in a loop and across calls. It prints 503335 on both binaries and both walks with `VYRN_LEAK_CHECK=1`, with no audit line.
- `VYRN_LEAK_CHECK=1 vyrn run` of `domdemo`, `fnvalstore`, `graphql`, `refutablelet`, `mandelbrot`, `stream`, `streamops`, `streamunfold` and `streamlazy`, main's binary and the tip's, core walk on and off: the same stdout, empty stderr, exit 0. `vyrn bench --check` under the audit: `membench` 22 ok, `jsonplace` (whose `ensureDoc` runs only in benches) 2 ok.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `kernel --ignored`: 27,061 accepted, 0 refused, 0 unlowered.
- `scripts/check-corpus.sh`, main against the tip: 493 shared roots byte-identical, the shape gained and accepted (418 accepted, 76 refused).
- `cargo nextest run --release -p vyrn-cli`: 669 passed, 45 skipped. `cargo test -p vyrn-lower -p vyrn-codegen` green. Both formatters clean; the release build has no warning. Re-pinned per commit: `emitter-census` the mapping 11,924 to 11,915 to 11,930, one block per builtin name 4,421 to 4,448; `emitter-reads` both, for two questions 9,782 to 9,773 to 9,756, the core's rows 2,983 to 3,042; `surface` `Type::Str` 81 to 80, `Type::Int` 64 to 65.
Findings:
- the arm validates a literal under a `where` annotation (`let home: Section = "home"` calls `where$cSection`), which the checker proved; the core walk does not. The core walk validates `t("nav.\{about}.label")`, which the checker proved; the arm does not. Neither walk states the checker's proof as one fact.
- `merge=pin` keeps the upstream side of `wasm-sha256.tsv` on a rebase, so a rebased slice drops the manifest rows it wrote; they were written again by hand into each commit.
Left: `finitekeys` `main`, blocked by the core walk's check at a proven String crossing; the logger bodies, blocked by `Let/Call/Builtin/logger`; `Store/Elem` of a user container, blocked by RFC-0120's resolution at the site.
