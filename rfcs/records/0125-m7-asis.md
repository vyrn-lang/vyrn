# One predicate says whether a value is its destination's type as is (2026-09-26, `m7-atrhs`)
RFC-0125, milestone M7, on the deletion branch.
Decision: the lead's, from m7-stream's finding in #pages (89c1fa79). Six screens compared `resolve(a) == resolve(b)` one level deep. Each asks `Fn_::core_as_is(from, to)` instead: the coercion plan's `Identity` or `FnRetag`. `Fn_::core_returns_as_is` became that predicate with the result type as `to`.
Went: the comparison in `core_alias`, `core_copies`, `core_renames`, `core_run`'s frame against the row, an aggregate call's result against its binding, and `annotated_apart`; `annotations` keeps the annotation as written. Stayed: `core_run`'s `let`-annotation clause, which compares as written for a `where` type.
Lines: `direct.rs` 17,877 to 17,874. Refusals: 0 lost / 0 gained. Manifest: untouched.
Licence, base `origin/m7-delete` `5fac9c8c`:
- CLI suite, manifest check: 6 failing before, the same 6 after (benching 5, derived 1). The two `emitter_census` pins moved and are re-pinned.
- `wasmhash` with the manifest check: pass before and after.
- the 136 shapes wrapped as coredrive wraps them and the 206 examples, built to wasm by both binaries: the same refusals, and byte-identical modules for every one that builds. No site stops being refused, so no shape is added.
Time: about 70 minutes: 10 reading, 5 the change, 55 gates on two bases (m7-pages-del first, then `origin/m7-delete`).
