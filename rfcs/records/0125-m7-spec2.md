# A parameter bound to a stored value is that value, and a lambda's key and capture order are stated once (2026-09-25, `m7-spec2`)
RFC-0125, milestone M7.
Decision: the lead's, four slices: `Target::Value`, a lambda target passed on with its captures, #459 (the key names its column), and group C (one capture order).
Went: the line-only literal locator (`Fn_::literal` by owner and line); the closure row carries its key, `Op::Closure(key)`. `lambda_captures` from `vyrn-codegen/src/lib.rs` with its descent macro; the core's scope-order walk in `Builder::captures`; the by-name reordering in `Fn_::core_make`; `core_lambda`'s count check, which the shared order makes always true; the callee list of `mentions_in_lambda`, which nothing read. The effects test's copy of the key parser, which read `57:23` as no line and left 21 calls unattributed.
Stayed: `effects.rs` still keys lambda frames by line, which is what the checker records (`StoredLambda::line`).
Lines: `direct.rs` 21,895 to 21,902; `lib.rs` 1,847 to 1,771; `core.rs` 9,327 to 9,395; `effects.rs` 699 to 689; `tests/effects.rs` 1,088 to 1,078. Group C alone: `direct.rs` -49, `lib.rs` -76, `core.rs` +55. Refusals: 0 lost / 0 gained. Manifest: 5 rows in 4 commits, below.
Licence, main `0f499e7b` against the tip `17d22a4c`:
- `coredrive --ignored` with the manifest check: taken 21,082 to 21,092 of 21,172 (+10, the prediction; +10 on `6ad6dfc2` and `31a3e69a` too); carried end to end 1,664 to 1,666; 1 byte-identical, 167 run the same, 0 run apart. On `b640e67d` the slices took 21,033 to 21,042 (the first three) to 21,043 (group C).
- manifest, each row written in its commit and named from `wasm2wat` with `VYRN_WASM_NAMES=1`: `closures2` and `fnvalarg` (`Target::Value`: `apply`, `sortBy`, `map`, each one `memory.copy` fewer); `lambdas` (`mapThenSum`, +3 wat lines); `closures2` again (#459: `main`'s two-lambda `Ops` literal is built in place, frame 464 to 432 bytes, 2 `memory.copy` fewer); `fnvalstore` (group C: `@lambda_wrapSink`, +3 lines, one dead `br`). No other function in the four modules moved.
- `kernel` 27,061 accepted, 0 refused, 0 unlowered, as on main. `effects` 29,619 judged, 119 through a value, 0 unattributed, 0 differ. `typed` 236,909 judged, 0 unjudged. `coretables` green.
- parser: `cargo nextest run --release -p vyrn-cli` 674 passed (diagnostics pin, formatter re-lex); the lowering pin `lowered_dump --ignored` green, no pin moved; `cargo test --release --manifest-path vyrn-lsp/Cargo.toml` 23 + 77 passed.
- `check-corpus.sh` against `N:/wt-core` at `0f499e7b`: `diff -r` shows only the two new shapes, both accepted with exit 0; 442 accepted, 77 refused.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking. `VYRN_LEAK_CHECK=1` on the four examples and six lambda shapes, main's binary and the tip's, engine and native route: identical output and exit code, no audit line (`closures2` ends in its recorded division-by-zero trap on both).
Time: about 210 minutes: work 50, gates and per-commit pins 110, three rebases onto a moving main 30, a timed-out base run 20; a resume after a power loss, two rebases, every commit re-pinned and the gates again 110.
Findings:
- `tests/effects.rs` held its own parse of the lambda key; #459 broke it silently until the ignored suite ran. It now calls `core::lambda_line`.
- two lambdas in two interpolation holes on one line still share a key, because `parse_hole` re-lexes from line 1 (#471, commented).
- a rebase with `--autosquash origin/main` moves the base when main has advanced; the pins then take main's side and every commit's rows need writing again. Rebase fixups onto a fixed base.
- killing a build mid-link left the incremental cache naming symbols it no longer held (LNK2019 in `vyrn_frontend`); `cargo clean -p vyrn-frontend` fixed it.
Left: #471, blocked by the parser fix it names.
