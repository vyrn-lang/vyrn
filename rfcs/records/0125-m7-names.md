#### A name the run reads is asked for its place and type, for a String, an array and a map (2026-09-24, `m7-names`)
RFC-0125, milestone M7.
Decision: the let, names and place clauses of `Fn_::core_run` admit a name whose type is a String, an `Array` or a `Map`, through `core_name_ty`; the place clause still asks that the two walks agree on the type. The lead's rule (brief `m7-names`); the order String, array, map is the count's.
The count, from a temporary instrument at the three non-scalar refusals of `core_run` (every refusal of a statement collected, none returned early), over `emit-wat` of the 174 programs of `examples/` and the 415 roots. On main `2cf48cbd`, statements first / only / met: record 3,053 / 2,653 / 5,585, Array 2,121 / 1,052 / 3,435, String 963 / 320 / 1,631, enum 174 / 39 / 393, Map 30 / 6 / 164. The track is built on `m7-iflet` (`0acfde6d`), because a String temporary's drop joins its statement's run there: on main the String commit left 3 blocks, 56 bytes, unfreed in `protocol.vyrn`.
Before and after, on `0acfde6d` and the tip, statements the screen refused, first / only: examples 7,706 to 5,559 (Array 1,686 / 1,138 to 31 / 18, String 996 / 446 to 2 / 0, Map 38 / 9 to 5 / 2, record 3,996 / 3,951 to 4,748 / 4,456); the 415 roots 22,342 to 14,562 (first: Array 5,808 to 32, String 4,793 to 13, Map 55 to 6, record 8,320 to 11,311).
Went: the scalar-only question of three clauses. Stayed: a record, an enum, a `Stream`, a function value and a `where` alias of a String or an array, because each needs more than a place (below).
Lines: `direct.rs` 21,265 to 21,272 (one helper, four call sites). `coredrive.rs` 990 to 1,013. Refusals: 0 lost / 0 gained. Manifest: 31, 20 and 8 rows, one commit each.
Licence:
- `coredrive --ignored`, base / String / array / map: taken 20,418 of 21,176 each, plus 74 judged; carried end to end 1,647; 1 byte-identical, 167 run the same, 0 run apart. The core's rows: `Stmt::Let` 2,353 / 2,500 / 2,189 / 2,195, `Stmt::Assign` 2,550 / 2,577 / 1,347 / 1,349, `Stmt::If` 676 / 711 / 763 / 763, `Stmt::Expr` 109 / 183 / 216 / 232, `Stmt::While` 5 / 5 / 190 / 190, `Stmt::ForIn` 0 / 0 / 2 / 6, `Stmt::IfLet` 0 / 0 / 1 / 4; the arm's `Stmt::While` 1,477 to 956. The pin of forms the rows carry gains `Stmt::ForIn` and `Stmt::IfLet`. `SHAPES` 57 to 60: a String name, an array name and a map name, each bound, read, passed and moved beside a lambda.
- the moved rows, per function from `wasm2wat` against the commit before: 41, 22 and 10 functions. Every one calls the same functions, and all but 8 in the same order. The shapes: a String temporary a call reads is set and got where the arm teed it; a temporary is built before the literal a concat pushes; a `push` of a copy copies before it grows and drops the arm's 24-byte self-copy of the receiver; a `for` over an array or a map's keys reads the header in place of the arm's snapshot, and checks each element read; two instances of `fnvalstore` swap indices. Frames shrank in `graphql` by 32 and `langbench` by 16 bytes (String), in `mapdemo` by 80 and `wirekey` by 32 (map), and none grew; the array commit's frames were not compared.
- every moved example under `VYRN_LEAK_CHECK=1`, the commit before against the commit: stdout, stderr and exit codes equal, 0 leak lines.
- `residue --ignored` at the tip: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed.
- `vyrn check` over the 415 roots, main's binary against the tip's: `diff -r` empty, 339 accepted and 76 refused.
- `kernel`: 175 programs, 27,066 accepted, 0 refused, 0 unlowered. `effects`: 29,624 judged, 0 unattributed. `typed`: 236,919 judged, 0 unjudged. `coretables` and the manifest check green.
- `cargo nextest run --release -p vyrn-cli`: 666 passed, 0 failed, 47 skipped. Both formatters clean; the release build has no warning. `emitter_census`: the mapping 11,699 to 11,706, `both, for two questions` 9,857 to 9,864. RFC-0126: `Str` 81 to 82, `Array` 40 to 41, `Map` 22 to 23 in wasm, 1,431 to 1,434 mentions.
- not run: `vyrn bench --check` and the bench corpus.
Time: about 150 minutes of work, 25 of gates, 0 waiting.
Findings:
- an array and a map are not `i32` pointers in a local, as the brief read them: each is a header layout in a slot (`{ ptr, i64, i64 }`, five words for a map). The core reads a layout name at its address, so the same clause holds for both.
- the rows' `for` over an array checks each element read against the length, which the arm's loop exit made redundant: one compare and branch per iteration, not measured.
- admitting a record the same way moved 37 examples; 36 ran the same with 0 leak lines, and `show.vyrn` failed to build: `str_value` has no reader for `toString` of a record, and the screen let the row reach it. Reverted, not committed.
Left: a record name, 4,456 statements over `examples/` refused only for it, blocked by the core's readers for a record read by value (`@str` of a record first); an enum name, 165, blocked by the same question for a sum; a `where` alias of a String or an array, 13 over the roots, blocked by the names clause asking the declared type, not the resolved one.
