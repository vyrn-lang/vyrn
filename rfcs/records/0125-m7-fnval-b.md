#### A call through a value reads the value, and the core walk calls it through its dispatcher (2026-09-25, `m7-fnval-b`)
Decision: the lead's, representation A (`Callee::Value(Name)`, the row names the value it reads) and decision (a): a `fn` value moves into a literal part as any value that can own heap does, so the kernel is right and the checker's silence is the gap (#470).
Went: the spelling lookup behind a `Value` call in `core::specialize`, `effects::callee` and the emitter (`core::through` and its one-name-per-spelling refusal were never merged); the emitter's refusal of a read of a deferred field; `lazyfield`'s `outside_main` carve-out (#452). Came: `Builder::force` (a lazy read is a named read of the closure and a `Value` call through it), `Callee::value`, `Fn_::core_through`, `Fn_::value_sig`, a dispatcher arm in `core_call`. Stayed: a `Value` call through a `fn`-typed parameter, which waits on the specialization and stays a gap.
Lines: `direct.rs` 21,411 to 21,476; `core.rs` 8,858 to 8,910; `kernel.rs` 3,076 to 3,080; `effects.rs` 693 to 686. Refusals: 6 gained, then 6 lost by the guide fix, net 0. Manifest: 14 rows, below.
`direct.rs` grows by 65 because the core walk gains its reading of a `Value` row (the dispatcher arm in `core_call`, `core_through`, `value_sig`, and `core_sig` taking the body), while the AST arm's `fnval_call` stays as long as the arm emits the 250 bodies the core walk refuses.
Licence, rebased onto main `069c4a49` (#466 and `m7-mapkey`); the instrument, corpus, wat and site rows were measured against `7dc011ee` before the rebase:
- `coredrive --ignored` with the manifest check: taken 20,906 on main to 20,927 of 21,177 at the tip (20,868 to 20,889 on `7dc011ee`); 1 byte-identical, 167 run the same, 0 run apart; the new shape "a call through a stored value" at 0 break, 0 continue.
- the census row "a `Value` call" with the screen instrument (uncommitted): 22 refused bodies met it, 13 alone, before; 0 after.
- `vyrn check` over the 491 roots with `scripts/check-corpus.sh`: every shared root byte-identical to `e3e6023a`, 10 gained shapes accepted; 415 accepted, 76 refused.
- `kernel` 27,066 accepted, 0 refused, 0 unlowered; `effects` 29,624 judged, 0 differ, 0 unattributed; `typed` 236,919 judged, 0 unjudged; `coretables` green; manifest check green.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking.
- the 14 moved rows are the same after the rebase; `langbench`, `pagesdemo` and `rest` are rows #466 moved too, and against main's module each differs only in this track's functions (`callN`, `runParamQuery`, `httpInvoke` and `httpInvokeWhole`). `wasm2wat` of `e120d5a9`'s module against the tip's: each is a body the core walk now takes. `memory.copy` fell in 11 functions (`lazyfield` 16 to 10, `rest`'s `httpInvoke*` 7 to 5, `runChain`, `runOps`, `pagesdemo`'s `runParamQuery`, `fnvalstore`'s `main`, `@lambda_unfold` in three programs, each by 1) and rose in none; the 4 `fnvalstore` `fire*`, `callN`, `zipApply`, `generics` and `fnvalarg` `main`, and `@lambda_map` grew 2 to 16 wat lines for the dispatcher's value operand.
- `lazyfield`: the whole module equal for the two spellings at the tip; on `fe47a8b9` the modules differ in 12 lines.
- `cargo nextest run --release -p vyrn-cli`: 669 passed, 0 failed. Both formatters clean; the release build has no warning. `emitter-census`, `emitter-reads` and `surface` re-pinned.
- the site export, `e3e6023a`'s binary, with and without the guide fix: the only difference is the changed source line, in `guide/functions.html` twice and `guide/functions.md` once.
Findings:
- soundness: `h.f` read into `g`, `swap(h)` storing into `h.f`, then `g(5)` was accepted on main and ran apart, 10 on the core walk against 6 on the AST walk. The kernel now refuses it ("`h` is written here while `g` still reads out of it"), witnessed in `refusals.rs`.
- the checker accepts a `fn` value used after a move into a literal part; the kernel refuses it. `site/guide/closures.vyrn` was the corpus's one program in that shape and copies its two values now. Filed as #470, not fixed here.
- the refusal's "used again here" line reads `line 1` for a use inside an interpolation hole, for any value, on main too: `Parser::parse_hole` re-lexes the hole and its nodes count lines from 1. It is not a `Value` row field. Filed as #471, the lead's decision.
- `e120d5a9` changed the gap tag to `Call:Value(N):f`; the census names `Call:Value:f`, and `b6a2b43f` restores it.
Left: a lambda as a target and `ho_call` and `fn_binds` with it, blocked by nothing this track found; the interpolation hole's line, #471, a parser slice licensed by the diagnostics pin, the lowering pin, the re-lex invariant and the LSP suite; #470, blocked by the choice of the move rule's one home.
