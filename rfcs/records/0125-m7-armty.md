#### The screen asks the row for a binding's type, and a region statement is its block row (2026-09-24, rebased 2026-09-24, `m7-armty`)
Decision: the type clause of `Fn_::core_run` reads a call's and a closure's binding type from the row's producer type, and `Stmt::Region` pairs with `St::Block { region: true }`, keyed by the block the row names. No row kind and no field. The lead's two clauses; the reading is mine, on the counts below.
The count, on `41ef02a0`, from a temporary instrument in `core_run` over `emit-wat` of the gate list's roots (examples, tests, repro), by the clause that stops the statement holding the arm: `Expr::Float` under the type clause 51 occurrences / 24 bodies (28 / 20 a lane builtin in `simdbench` and `simdmem2`, 23 / 4 a `@str` in `numbytes` and `repro`); a lambda under it 18 / 11 (a `Closure` bound to a function-valued alias, a `lazy String` or a function type) and 16 / 16 typed apart (a closure took its first capture's type). Statements the type clause stopped, by what they bind: a reserved call 856, a builtin 425, a user call 134, a call through a value 83, a read 68, a closure 47, a method 5, a literal 1; bodies first / only / met 226 / 112 / 1,017. `Stmt::Region`: 11 statements, `main` of `regionescape` (10) and `matchown` (1), all with no run keyed.
After, on the same base: the type clause stops 0 of these arms, 69 statements (68 reads, 1 opaque literal) and 17 / 6 / 43 bodies; typed apart 19 to 1 statements. The freed statements stop at a later clause: a binding that is no scalar 12,218 to 13,734. The 11 regions pair and stop at a made layout that is not the statement's binding (8, `m7-iflet`'s clause) or a binding that is no scalar (3).
Went: `core_arm_ty`'s call arm through `core_mem_ty` and `core_sig`, 10 lines. Stayed: the operator arm, because the arm's width rule is the divergence the clause exists for.
Lines: `direct.rs` 20,937 to 20,937. `core.rs` 8,565 to 8,571 (`Body::node_of` names a region's block). `coredrive.rs` 876 to 897. Refusals: 0 lost / 0 gained. Manifest: untouched.
Licence at the tip, rebased onto main at `0e9f0403` (after `m7-store` and `m7-fnval`) on 2026-09-24, pins taken from main and re-pinned per commit; the call arm the first commit deletes had gained `m7-fnval`'s `targets`, and it went with the arm: `coredrive --ignored` taken 20,418 to 20,418 of 21,176, plus 74 judged; carried end to end 1,647; 1 byte-identical, 167 run the same, 0 run apart; `SHAPES` 53 to 56. `direct.rs` 21,199 to 21,198, `core.rs` 8,755 to 8,761, `coredrive.rs` 951 to 972. Manifest untouched. `emitter_census` the mapping 11,635 to 11,634, `both, for two questions` 9,793 / 104 / 290 to 9,792 / 106 / 292; RFC-0127 859 to 861. `vyrn check` over the 415 roots against main's binary: `diff -r` empty, 339 accepted and 76 refused. `kernel` 27,066 / 0 / 0, `effects` 29,624 judged, 0 unattributed, `typed` 236,919, 0 unjudged, `coretables` and the manifest check green. nextest CLI 666 passed, 0 failed, 47 skipped; both formatters clean, no warning.
Licence, before the rebase:
- `coredrive --ignored` on `2e7a7733` and on the tip: 168 programs, 21,146 bodies, taken 20,250 of 21,176 plus 74 judged, carried end to end 1,637, 1 byte-identical, 167 run the same, 0 run apart; every per-form count equal. `SHAPES` gains an extern call bound beside a statement the arm emits, and two regions beside one; each takes the same module as the arm.
- `vyrn check` over the 415 roots, `N:/wt-core`'s binary against the tip's: `diff -r` empty, 339 accepted and 76 refused.
- `kernel --ignored`: 175 programs, 27,066 accepted, 0 refused, 0 unlowered. `effects`: 29,624 judged, 0 unattributed. `typed`: 236,919 stores judged, 0 unjudged. `coretables` and the manifest check green.
- `cargo nextest run --release -p vyrn-cli`: 666 passed, 0 failed, 47 skipped. Both formatters clean; the release build has no warning. `emitter_census`: the mapping 11,402 to 11,395 to 11,402 lines, `both, for two questions` 9,584 / 104 / 286 to 9,584 / 106 / 288. RFC-0127's table: `Stmt::Region` 13 to 15 mentions, 858 to 860.
- not run: `residue --ignored`, `vyrn bench --check` and the bench corpus; the fast gate list replaces them, and no manifest row moved.
Findings:
- the clause asked the arm's callee table, which knows no lane builtin, rendering builtin, extern or function value, so it refused 1,550 statements that the row types.
- a closure's binding took the type of its first capture: 16 lambdas were typed apart for that reason alone.
- no statement moved to the core: each one freed stops at a later clause of the same statement.
Left: the 68 reads the type clause still stops, blocked by `core_place_ty` giving no type for their place (not instrumented further); the 11 regions, blocked by `m7-iflet`'s made-layout clause and the scalar clause.

The binding-type track (`m7-armty`) changed no gap and moved no body. The type clause of the emitter's screen asked the arm's callee table, and refused 1,550 statements whose rows carry the type; it reads the row since this track, and a region statement pairs with its block row. Each statement freed stops at a later clause of the same statement, most at a binding that is no scalar, so `coredrive --ignored` reads 20,250 bodies taken, as on main.
