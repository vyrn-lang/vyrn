#### The checker's proof of a crossing is asked once, and a proven crossing is a row that checks nothing (2026-09-25, `m7-where`)
RFC-0125, milestone M7.
Decision: `validate::proven(e, to)` beside `consteval::eval` and `finite::string_flow_proven` is the checker's record, and both walks ask it; the builder states a crossing into a validated type as the type's constructor row, `Callee::Named` where a check is owed and `Callee::Proven` where the checker proved it. The lead's. One reading is mine and differs from the brief: a record literal of a validated type is not a crossing (`validate::required` says so), so a proven one states no row, and one the checker did not prove gets a `Named` row after the make that reads the layout in place.
Went: the checker's three statements of a constant's verdict (`prove_coercion`, `check_struct_lit`, `check_construction`), now `validate::constant_verdict`; the arm's own tests at the record literal, `T(v)` and `coerce`; the builder's literal short cut in `check`; the emitter's refusal of a validated record literal, of a checked layout as an argument, and of a checked layout result returned whole.
Stayed: a crossing the checker did not prove at an argument, a return, a part or a store through a place, which the emitter still checks at the operand (`Fn_::coerce`), because stating it as a row adds a local per site on every dynamic crossing in the corpus. A borrowed heap value crossing into a validated type, because the constructor would take what a plain binding borrows.
Lines: `checker.rs` 14,754 to 14,709. `validate.rs` 149 to 190. `core.rs` 9,665 to 9,815. `direct.rs` 22,045 to 22,088. `typed.rs` 1,148 to 1,159. Refusals: 0 lost / 0 gained. Manifest: 19 rows, named below.
Licence, on main at `4f8a57e6`:
- `coredrive --ignored` with `VYRN_WASM_MANIFEST=check`: taken 21,094 to 21,099 of 21,172 (the five bodies: `autovalidate` main, `inlinewhere` main and `mk`, `finitekeys` main, `mapdemo` main); whole 21,039 and carried end to end 1,666, unchanged; 1 byte-identical, 167 run the same, 0 run apart. Two new shapes, both 0 break, 0 continue.
- `check-corpus.sh`, main against the head: 524 common roots byte-identical (445 accepted, 79 refused); the head adds the two shapes, both accepted.
- `kernel --ignored`: 175 programs, 27,061 accepted, 0 refused, 0 unlowered. `typed`: 237,084 stores judged, 0 unjudged; by literal 9,202, by constructor 17. `effects`, `coretables`, `wasmhash` (check): green. `residue --ignored`: engine 173 clean, 0 leaking; route 173 clean, 0 leaking.
- `VYRN_LEAK_CHECK=1` over the 19 moved examples under wasmtime: stdout and exit code equal to the plain build, no leak line. `jsoncodec` exits 4 on main too.
- witnesses, main's binary against the head, same bytes: `let home: Section = "nope"`, `Age(3)` and `Range { start: 9, end: 2 }` refused with the same sentences; `n + 20` into `Adult`, `older(n + 2)` from 5, `mk(5)` into `User.age`, `mk(160)` against `User`'s `where` and `mkR(n + 20)` against `Range` trap with the same message on both engines; an inner `let s = "bad"` shadowing a proven `s: Side`, passed to `side(s: Side)`, traps.
- `cargo nextest run --release -p vyrn-cli`: 677 passed.
Rows, from `wasm2wat` of main's binary and the head's, as check calls main to head; every call that went checked a constant or a contained string flow, and a pair is a type's constructor and predicate that lost their last caller: `graphql` 25 to 8; `autovalidate` 9 to 3; `jsoncodec` 6 to 2; `schemaimport` 5 to 2 and `Port`; `rest` 8 to 4, three `Path*` pairs and `regexRun`, 139,991 to 118,271 B; `rpc` 4 to 1 and `Age`; `rpcsplit` 4 to 0 and `Id`; `i18ndemo` 12 to 7, `NavSection` and `TStatusState`, 121,749 to 87,238 B; `inlinewhere` 5 to 2 and `User.name`; `pagesdemo` 4 to 2; `vlog` 4 to 2; `finitekeys` 4 to 0, `Section`, `TransKey` and `regexRun`, 72,602 to 4,106 B; `twdemo` 1 to 0, `Tw` and `regexRun` with its table, 1,245,690 to 44,421 B; `gendemo`, `mapdemo`, `modules` and `namespace` 1 to 0 and one pair each. `stringops` and `regex` lose 1 and 3 frees of static data at `T("literal")`. `mapdemo`, `autovalidate` and `inlinewhere` also moved because their bodies moved to the core walk; `mk` builds in a 16-byte slot and copies, because the check row reads the make, so it no longer lands in the caller's storage.
Time: 185 minutes: 70 work, 75 gates (three `coredrive` runs, 245 to 500 s each on a loaded machine), 15 two rebases, onto #493 to #500 and #501 to #505, 10 lost to a build and gate over `timeout 600`, 15 reading the previous agent's diff.
Findings:
- the arm skipped the check of a record literal and of `T(v)` whenever the operand was constant, proven or not; it asks `proven` now, so a constant whose predicate does not fold is checked.
- the core walk checked every proven crossing: `core_val` coerces with no expression, so `coerce` never saw the proof. That is where most rows above moved.
- `T("literal")` bound an owned name and freed static data at scope end; the constructor over a literal is static now.
Left: a checked layout part of a record (`Pair { r: Range { .. } }`), blocked by `core_payload_layout` and `core_part_at` refusing a validated part type; a crossing the checker did not prove at an argument, a return, a part or a store, blocked by the cost of a row per dynamic site, which needs its own decision.
Census: `checker-census` the typing judgment 3,247 to 3,223 lines, a rule the checker states 1,467 to 1,446; `emitter-census` the mapping 12,325 to 12,374; `emitter-reads` both 10,087 to 10,135 lines, 321 to 327 rows; `forms` `Expr::StructLit` 16 to 15; `surface` `Type::Named` 69 to 74; the coercion census's `prove_coercion` 44 to 25 lines.
