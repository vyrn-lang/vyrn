#### A parameter bound to a lambda is that lambda's captures (2026-09-25, `m7-spec`)
Decision: the lead's, rule B stated once in `core::specialize` with `Target::Lambda`, and a lambda key two bodies share gives no target (#459). Mine: the target carries each capture's name and type from `FnBinding::cap_srcs` and the lambda's signature, so the capture order has one home.
Went: four screen clauses that asked `n < params.len()` for "n is a parameter", which a specialization had already made false. Came: `Target::Lambda`, the capture splice in `specialize` and `bind_targets`, `core_target_of`, `Cx::lambda_key`, `Cx::lambda_sig`. Stayed: a pass-through of a lambda-bound parameter (`mapThenSum`), because `core_target` names no lambda; a lambda that captures a bound parameter (A1 below), behind the `lambda` clause.
Lines: `direct.rs` 21,476 to 21,507; `core.rs` 8,910 to 8,948. `direct.rs` grows by 31 for the key lookup and the resolver, which have no AST twin to delete: the AST arm reads `fn_binds` directly. Refusals: 0 lost / 0 gained. Manifest: 2 rows, below.
Licence, at the tip, on `87037b07`; rebased onto main `6406139e`, coredrive reads 20,922 on main and 20,931 of 21,172 at the tip, the manifest moves only `capturefn` and `lambdas`, nextest 669 passed, residue 173 clean on both engines:
- `coredrive --ignored` with the manifest check: taken 20,927 to 20,936 of 21,177; 1 byte-identical, 167 run the same, 0 run apart. New shapes: a parameter bound to a lambda with two captures (51; 39 with the captures swapped), and two lambdas on one line as targets (12018, where calling one lambda for the other gives 12004 or 90018), each at 0 break, 0 continue.
- the prediction was 20,942 (13 to 16 more). It missed by 6: 6 of group B's 17 are bound to a stored value's dispatcher, not a lambda (`fnvalarg` `apply`, `callWith` and `map`, `shadowing` `applyTo`, `closures2` `apply` and `sortBy`). The census read "no named target, captures > 0" as a lambda, and a dispatcher is a target with one capture. Of the 11 bound to a lambda, 9 are taken; `applyAll` makes a lambda of its own, and `mapThenSum` passes its parameter on.
- the 9 bodies taken: `lambdas` `map` x3, `filter`, `fold`, `any`, `firstOf`; `capturefn` `map` x2. The two `capturefn` bodies needed the parameter clause fix: their capture names come after the parameters.
- manifest: `capturefn` and `lambdas`, `wasm2wat` against `87037b07`: exactly the 9 functions; `memory.copy` fell in 7 and rose in none; each grew 1 to 13 wat lines. The refactor commit alone moves none of 206 examples.
- #459: `closures2`'s two `@lambda main` bodies stay on the arm, and so do both `twice` instances in the new shape (the instrument showed them unbound).
- `vyrn check` over 494 roots: shared roots byte-identical to the `m7-fnval-b` tip, 3 gained shapes accepted; 418 accepted, 76 refused.
- `kernel` 27,066 accepted, 0 refused, 0 unlowered; `effects` 29,624 judged, 0 differ; `typed` 236,919 judged, 0 unjudged; `coretables` green.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking. `VYRN_LEAK_CHECK=1` on `capturefn`, `lambdas` and the two shapes, engine and native route, `87037b07`'s binary and the tip's: same output, exit 0, no audit line.
- `cargo nextest run --release -p vyrn-cli`: 669 passed. Both formatters clean, no warning. `emitter-census` and `emitter-reads` re-pinned per commit.
Findings:
- the 38 bodies `core_body` turned away, by why the parameters differ: A, 20 with a named target whose parameter is still read after binding (A1, 18: a lambda captures it, `std/stream` `map`, `filter`, `unfold` x14 and `capturefn` x4; A2, 2: `defer` stores it in a record); B, 17 with no named target (11 lambdas, 6 dispatchers); C, 1 reorder, `fnvalstore`'s `wrapSink@lambda` (core `[n, sink, m]`, frame `[sink, n, m]`).
- no group B body has a capture order that disagrees between the core and the frame.
Left: the `lambda` clause, 22 bodies first, with the 18 of A1 behind it; a pass-through of a lambda target (`mapThenSum`), blocked by `core_target`; the 6 dispatcher-bound bodies, blocked by a `Target` for a dispatcher; A2, blocked by a bound parameter read as a value; C, with #459.

The starting census, measured on `87037b07` with the screen instrument (uncommitted), over the 250 bodies coredrive counts as refused. Each row is a clause: bodies it is first in, alone in, and met in.

| clause | first | alone | met |
|---|---|---|---|
| no-core: no instance | 43 | 43 | 43 |
| name with no place: Array, Read | 33 | 0 | 33 |
| lambda | 22 | 0 | 22 |
| name with no place: Array, Val | 22 | 0 | 23 |
| name with no place: Stream, Call | 17 | 0 | 22 |
| name with no place: Record, Read | 16 | 0 | 16 |
| stmt: If | 13 | 0 | 18 |
| no-core: has a body | 10 | 10 | 10 |
| stmt: Let, Read | 10 | 9 | 90 |
| stmt: Let, Call, Builtin | 9 | 1 | 45 |
| name with no place: Enum, Read | 9 | 0 | 11 |
| name with no place: Map, Call | 6 | 0 | 6 |
| stmt: Do, Call, Builtin | 6 | 6 | 25 |
| stmt: Store, Global | 5 | 5 | 6 |
| name with no place: SmallArray, Make | 4 | 0 | 4 |
| stmt: Store, Key | 4 | 4 | 6 |
| stmt: Let, Call, Method | 3 | 3 | 4 |
| stmt: Let, Call, Reserved | 3 | 2 | 21 |
| name with no place: Array, Call | 3 | 0 | 7 |
| name with no place: Record, Make | 2 | 0 | 3 |
| stmt: Store, Elem | 2 | 0 | 11 |
| stmt: Let, Call, Fn | 2 | 2 | 8 |
| name with no place: Record, Val | 2 | 0 | 2 |
| name with no place: Map, Make | 1 | 0 | 1 |
| annotates a check | 1 | 1 | 1 |
| checked aggregate return | 1 | 0 | 1 |
| name with no place: Record, Call | 1 | 0 | 2 |

The two no-core rows are 53 bodies: the 38 above, 13 `Owned__Slots__release` with no core body, and `closures2`'s two lambdas on one line (#459).
