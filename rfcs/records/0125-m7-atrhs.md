#### `a[i]` and `a[i] = v` on a user container are the projection's on the core's path (2026-09-25, `m7-atrhs`)
RFC-0125, milestone M7.
Decision: the lead's brief (`m7-atrhs`): a declared projection answers `a[i]` before the builtin row, in `Builder::call`, as `Checker::expr` asks `place_result` first. The store half (the `m7-window` record's Left item) and the refusal wording (option A) are this track's, reported to the lead before building.
Count, with the screen instrument and a marker in `Builder::call` and `Builder::index_set`, on `c86af1ee`: 9 refused bodies hold a Builtin `@at` or a bare `IndexSet` on a receiver a projection answers for. `projection` `main` met only `@at`. `autorelease` `main`, `copy` `handles__from0`, `genref` `bump` and `main`, `membench` `slotsChurn` met only `@at` and the bare store. `container` `main` (two `Let/Read`), `jchain` `main` and `slots` `main` met other clauses. Prediction: +1, then +5. Measured: +1, then +5.
Went: the Builtin `@at` row on a user container; the bare `Place::Elem` store into one; `coredrive`'s `PIN` row for `jchain.vyrn`. Stayed: `container` `main`, `jchain` `main`, `slots` `main`, on their other clauses.
Lines: `core.rs` 9,327 to 9,334 to 9,352; `typed.rs` 1,127 to 1,131. More than it deletes: `Builder::yielded` takes the store's parts instead of a statement, and answers the value a read-modify-write stores. Refusals: 0 lost / 0 gained over the corpus. Manifest: 7 rows, then 6, below.
Licence:
- coredrive with `VYRN_WASM_MANIFEST=check`: taken 21,053 of 21,172 on main `6ad6dfc2`, 21,059 at the tip on `6ad6dfc2` and on `31a3e69a`; on `b640e67d` 21,033, 21,034 and 21,039 per commit. Carried end to end 1,656 to 1,658. 1 byte-identical, 167 run the same, 0 run apart. Both shapes read 0 break, 0 continue.
- rows, per function from `wasm2wat` with `VYRN_WASM_NAMES=1`, main's binary against the tip's. Commit 1: `projection` `main` 850 to 895 lines (whole body, joins in locals); `main` of `autorelease`, `container`, `genref`, `jchain` and `copy` `handles__from0`, `membench` `slotsChurn` move by 2 to 16 lines where the per-statement walk takes the inlined read in place of the arm. Commit 2: `autorelease` `main`, `copy` `handles__from0`, `genref` `bump` and `membench` `slotsChurn` each 2 `memory.copy` fewer and a frame 16 to 32 bytes smaller (224 to 208, 208 to 176); `genref` `main` 21 to 13 copies, frame 704 to 576; `container` `main` and `slots` `main` move by statement.
- `vyrn check` over the corpus, main `31a3e69a` built in this worktree against the tip: the 507 shared roots are byte-identical, 430 accepted, 77 refused. The two new shapes are accepted.
- refusals the corpus does not reach, main `31a3e69a` against the tip: `s[h] = 5`, `s[h] = s[h] + 1` and a `Slots` parameter's `s[h] = s[h] + 1` say "cannot store into `s`" on both. `s[h].value = 5` and a user container's `b[0].x = 9` said "cannot mutate a field of" on main and say "cannot store into" at the tip, which is what the checker said before #489 (checked with a pre-#489 binary). `refusals.rs` witnesses both user-container forms.
- kernel 27,061 / 0 / 0 on main and at the tip. residue: engine 173 clean / 0 leaking, route 173 / 0.
- effects: pure 9,945 to 9,944. `genref` `bump` stops reading as pure: its store now runs `atSet`'s prologue, which can panic.
- `VYRN_LEAK_CHECK=1 vyrn run` of the 11 examples that name `Slots` and of `container`, `jchain`, `projection`, main's binary against the tip's: stdout, stderr and exit code equal, no audit line.
- pins: `forms.tsv` `Stmt::SetField` and `Stmt::IndexSet` in `lower` 10 to 9 and 11 to 10.
Time: 470 minutes: 90 work, 300 gates on a loaded machine (50 of them rerun at the tip on `31a3e69a` after a power loss), 3 rebases (main moved from `c86af1ee` to `31a3e69a`), 80 measuring main at each base.
Findings:
- `jchain`'s `break` stood on the clone the emitter's inlining of `Json`'s `at` held; with `@at` inlined by the core it is a core row and `PIN` is empty.
- the checker's expansion of `c[h] = c[h] + 1` stores a temp it binds before the container moves out, so `yielded` answers that temp as the value.
- #489 moved the store rule to `typed::stores`, which words a refusal by the first step out of the root. A place through a projection starts with the container's own field, so `s[h].value = 5` on an immutable `s` said "cannot mutate a field of" on main. `stores` asks whether a projection answers for the root's type and words it as an element store.
- `lowered`'s compared-answers floor: 4,223 on main, 3,975 after commit 1, 3,014 at the tip, because only the AST walk answers. The floor is 3,000 and names #465; the next track that takes a user-container body will cross it.
Left: a projected store into module state is worded by its first step, because `stores` has no global's type, blocked by no witness in the corpus. `container` `main`'s `c[3] = c[1]` second store and two `Let/Read`, blocked by the `Let/Read` clause. `jchain` `main`, blocked by `name-no-place` of the inlined `let JArr(items)`.
