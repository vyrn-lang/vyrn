#### A lambda the core makes is a function value its row builds (2026-09-25, `m7-lambda`)
RFC-0125, milestone M7.
Decision: a closure row `let n = closure(caps)` is a made layout, and the core walk builds it where the arm's `fnval_lambda` builds a stored lambda: the literal the row's line names in its owner (`ast::lambdas` carries the owner) is lifted through the arm's own `lift_lambda`, and `fnval_into` writes the tag and the capture block with its captures read through `Parts`, so the placement is stated once for both walks. The row lists its captures in scope order and the lifted signature takes them in the order the body reads them, so each part is read off the row by its source name. The lead's brief; the rows, `core::specialize` and `Target` are unchanged.
Went: the whole-body screen's `lambda` clause, which refused any body that holds a lambda frame; `fnval_into`'s own capture reads (`expr_as` over a list of expressions). Came: `Fn_::core_lambda` (the screen), a closure arm in `core_make`, `Fn_::lift_stored`, `Fn_::literal`; a closure row is a made layout in `core_makes`, `core_readable` and `core_part_at`.
Stayed: a lambda handed to a generic higher-order call (`map`, `fold`, `apply`, `sortBy`, `callWith`), which the arm monomorphizes; the call rows refuse, and the specialization is `m7-spec`'s. A stream `take` and `streammove`'s `stepped`, which wait on `fromStep` and `boxStream`.
Lines: `direct.rs` 21,518 to 21,646; `ast.rs` 2,448 to 2,456. `direct.rs` grows by 128 because the core walk gains a reader (`core_lambda` 49 with its doc, the `core_make` arm 32, `lift_stored` and `literal` 33, signatures and the `Parts` reads 14), while the arm's `fnval_lambda` stays for the bodies the core walk refuses. Refusals: 0 lost / 0 gained. Manifest: 11 rows, below.
Licence:
- the census instrument on 513bae71, before and after: 22 refused bodies met `lambda` first, every one also `name-no-place:Fn` and `stmt:Let/Prim/Closure`; 12 of them left the refused list (closures2 `addGuard`, `makeAdder`; fnvalarg `scale`; fnvalstore `main`, `onUserTagged`, `wrapSink`; langbench `main`; lazyfield `capturedAtConstruction`, `encodingForces`, `readTwice`, `shelf`; rest `routes`), and no body joined it. The prediction was +9, band +6 to +12.
- `coredrive --ignored` with the manifest check: taken 20,927 to 20,939 of 21,177 on 513bae71; 20,934 of 21,172 at the tip on 6406139e; 1 byte-identical, 167 run the same, 0 run apart; the new shape at 0 break, 0 continue.
- `kernel` 27,061 accepted, 0 refused, 0 unlowered; `effects` 29,619 judged, 0 differ, 0 unattributed; `typed` 236,909 judged, 0 unjudged; `coretables` and `wasmhash` green.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking.
- `vyrn check` over the corpus with `scripts/check-corpus.sh` against b144a6a9's tree: every shared root byte-identical, 417 accepted and 76 refused there; the new shape accepted.
- the 11 moved rows, `VYRN_WASM_NAMES=1`, `wasm2wat` against main's module, per function. Taken whole: closures2 `addGuard` (`memory.copy` 2 to 1) and `makeAdder` (1 to 0, 63 to 50 lines); fnvalarg `scale` (2 to 1); fnvalstore `wrapSink` (2 to 1), `main` (830 to 796 lines) and `onUserTagged` (same count, rewritten); lazyfield's four (`shelf` 7 to 3, the others 1 fewer each); rest `routes` (3 to 0). The closure's statement taken inside a body the arm walks: closures2 `main` (19 to 12), fnvalarg `main` (5 to 4), the `take` of membench, streamlazy, streamops and streamunfold (1 fewer each), streammove `stepped` (3 to 2), shadowing `shadowedByLambdaParam` (1 to 0). The shape is one: the arm builds the value in a scratch slot and copies it into the name's; the row builds it in the name's slot, or at the part's offset, or in the caller's storage. `memory.copy` rose in no function. langbench's module is byte-identical.
- the moved programs under `VYRN_LEAK_CHECK=1` with b144a6a9's binary and the tip's: the same stdout, stderr and exit code (closures2 exits 1 on its own division by zero on both), no leak line.
- the shape `a-lambda-whose-captures-are-read-in-another-order`: prints 3231 on the engine and on wasmtime, under `VYRN_LEAK_CHECK=1`.
- `cargo nextest run --release -p vyrn-cli`: 669 passed, 0 failed. Both formatters clean; the release build has no warning. `emitter-census`, `emitter-reads`, `forms` and `surface` re-pinned.
Findings:
- the `lambda` clause was a second statement of the closure row's refusal: every lambda frame has its closure row in the enclosing body, and `core_rhs_readable` refused that row too.
- `core::Builder::captures` captures a shadowed name beside the name that shadows it: `let x = 1 .. if .. { let x = 20 let f = y -> x + y + s.byteLength }` is the row `closure(x, s, x)` and the frame `(x, s, x, y)`. Two parts share the source name `x`, so `core_lambda` gives the body to the arm; the shape `a-lambda-that-captures-a-shadowing-name` witnesses it, and both walks print 3221 under `VYRN_LEAK_CHECK=1` with no leak line. The over-capture is the builder's, and it is not fixed here.
- the capture order agrees with `m7-spec`'s `Target::Lambda`: both come from `lift_lambda`'s capture list, which is `FnBinding::cap_srcs` at a call. A trial merge of 544268e3 onto this branch is clean.
- two lambdas on one line leave `Fn_::literal` with no answer, and the core already builds no body for them (#459).
Left: the builder's capture of a shadowed name, a `core.rs` slice licensed by the kernel corpus; a lambda handed to a generic higher-order call, blocked by `m7-spec` (a parameter bound to a lambda target in `core::specialize`); `fromStep` and `boxStream` with a closure argument, blocked by the builtin rows for a stream built from a step.
