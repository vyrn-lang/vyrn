#### A name of any type has a place, so the statement screen no longer asks its type (2026-09-24, `m7-names2`)
RFC-0125, milestone M7.
Decision: `Fn_::core_run`'s let, annotation and place clauses stop asking `core_name_ty`, the names clause and the `returned` list go whole, and `core_name_ty` goes with them; a name's place and its type on both walks are the whole question. The lead's rule (brief `m7-names3`), rebuilt on main after the power cut, because the first build on `0acfde6d` lost its `coredrive.rs` edit to NUL bytes; built on `a7a45dd7`, then rebased onto `5d479ffc`, which holds m7-render's step 1.
Went: the names clause (11 lines), the `returned` list (6), three `core_name_ty` asks, `core_name_ty` itself. Stayed: `core_scalar`, which the `return` form and the whole-body walk still ask. `1848f136` (the names clause resolves first) was not carried: the clause it fixed is gone, and its one row, `autovalidate.vyrn`, moves in this commit.
Lines: `direct.rs` 21,269 to 21,237. `residue.rs` 282 to 329, because the parallel run needs a per-program verdict. `lazyfield.rs` 249 to 253, `coredrive.rs` 561 to 562. Refusals: 0 lost / 0 gained. Manifest: 49 rows, below.
Licence:
- `coredrive --ignored`, `a7a45dd7` against the tip (m7-render's row reached no emitter, its record says): taken 20,418 of 21,176 both, 1,647 carried end to end, 1 byte-identical, 167 run the same, 0 run apart. The core's rows: `Stmt::Let` 2,195 to 2,629, `Assign` 1,349 to 855, `Return` 507 to 545, `If` 763 to 1,661, `Expr` 232 to 385, `While` 190 to 858, `ForIn` 6 to 40, `IfLet` 4 to 5, `Continue` 2 to 0. The arm: `Let` 1,519 to 1,025, `Assign` 1,164 to 254, `Return` 1,299 to 368, `If` 1,821 to 915, `Expr` 500 to 314, `While` 956 to 260, `ForIn` 144 to 108, `IfLet` 33 to 30, `Expr::Var` 14,248 to 8,026. The pin of forms the rows carry loses `Stmt::Continue`: its two statements sit inside a `while` the rows now take whole, which is read off the arm's count staying 0 and not traced. Shapes 61 to 66, each at 0 break, 0 continue.
- the 49 moved rows, named per function from `wasm2wat` with `VYRN_WASM_NAMES=1` against `a7a45dd7`'s binary, and classed by a temporary instrument that printed, per statement taken, the type of each name the old clauses refused (not committed). By the rarest class a row holds: a function value 4 (`closures2`, `fnvalarg`, `fnvalstore`, `generics`); a `SmallArray`, an `ArrayN` or an `F32x4` 3 (`copy`, `autovalidate`, `simdmem2`); a `where` alias of Int64, String or Map 6; an enum 17; a record 19. A record name is in 44 rows, an enum in 23.
- the wasm shapes: in 25 rows the rows build a layout in place where the arm copied it (`memory.copy` 1 to 9 fewer per function; frames shrank, `gqlAnswer` 320 to 192 bytes, `closures2` main 608 to 528, the std JSON decoders 160 to 128); in 3 more rows a frame grew (below); in 21 rows the rows stage an operand in a local where the arm left it on the stack (`local.tee` to `local.set` and `local.get`, a few bytes more). The 14 rows that hold `ksAdd` and `parseJson` of `std/json` are both shapes.
- the five new shapes, one per class, each emit a different module from `a7a45dd7`'s binary except the `where` one, which the rule admits and both walks emit to the same bytes.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed; 556 s serial (the lead's run on `0acfde6d`) to 51 s and 64 s on twelve threads (two runs). `fixtures --include-ignored` 3 passed. `VYRN_WASM_MANIFEST=check` green.
- `vyrn check` over the corpus roots, main's binary against the tip's: the 476 shared roots equal; 5 gained, the shapes, all accepted.
- `cargo nextest run --release -p vyrn-cli` 666 passed, 0 failed, 45 skipped. Both formatters clean; the release build has no warning. `emitter-census` the mapping 11,714 to 11,682, `both, for two questions` 9,871 to 9,839 (on `3f436607`); `surface` wasm 485 to 482 (`Str`, `Array` and `Map` one each).
Findings:
- on `a7a45dd7`, `show.vyrn` failed to build with the rule alone (`no lowering for toString of Point`), and a render reader that asked `ftypes::renders` (`23743080`, carried as `8c76712b`) fixed it. On `5d479ffc` the core states that render as a call to the impl, `show.vyrn` builds and runs without the reader, and the reader went. Without it the rows take 3 more `Stmt::Expr`, and coredrive still runs every program the same; which 3 the reader refused was not traced. `show.vyrn`'s row moved again (4 `local.tee` to `local.set` and `local.get`).
- `lazyfield`'s byte-identity claim broke in `main` only: the explicit program's `let f = b.body` is a place the rows give the closure, with 16 frame bytes and one `memory.copy` fewer, and the lazy force stays the arm's copy. The test compares every function but `main`.
- a sum argument costs a copy: in `assoctype`, `nestedsum` and `protocol` the rows copy an enum binding into a fresh 16-byte temporary before a call that reads it, where the arm passed the binding's address (`protocol` `main`, `nothing.show()`: frame 48 to 64, one `memory.copy` more; `assoctype` 112 to 144, two; `nestedsum` 304 to 320, one). Not measured for time.
- `lowered`'s has-type floor: 138 to 48 answers, and the floor went from 50 to 20.
Left: the sum argument's copy (#451), blocked by the rows' call reader, which reads a sum argument by value and not at its place; `lazyfield`'s comparison outside `main` (#452), blocked by the lazy force, which is the arm's.
