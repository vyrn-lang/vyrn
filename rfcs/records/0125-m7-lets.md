#### A generic impl call carries its type arguments, and a map membership test has a row (2026-09-25, `m7-lets`)
RFC-0125, milestone M7.
Decision: the lead's. Take the `let` clauses of the whole-body screen, one group per commit, largest first. G1 (`a[i]` of a declared `at` projection) went to `m7-window`. G2 and G3 are here.
Count, on `513bae71` with the screen instrument and a per-thread gate that drops the walks of a generator's own compile: 17 bodies are refused by a `let` clause and by nothing else. G1 is 10 (`std/stream` `cursorGet` and `srcOf` in 4 programs, `genref` `measure`, `projection` `main`). G2 is 5 (`assoctype` `main`, `falliblegeneric` `twice` and `shout`, `protocol` `main` and `describe`). G3 is 2 (the `json$d..` decoders of `mapdemo` and `wirekey`).
- G2: the builder named a generic impl function with `solved: []` in three places: a dispatched method (`dispatched` filtered generic impls out), the `?` on a `Fallible` sum (`fallible_try`), and a `for` over an `Iterate` container (`length_of`). `Builder::impl_args` solves the impl head against the receiver with `types::solve_param`, which `under_impl` also calls in place of its own `App`-only walk. A call the checker solved keeps the checker's answer.
- G3: `@has` had no builtin row. `Spec::Finds` states the map and the key. The key setup and the scan are `Fn_::map_find`, which `map_at`, the arm's `map_method` and the new row share.
Went: `under_impl`'s hand-written `App` match (G2); the second and third copies of the map key setup (G3). Stayed: `@remove`, which the arm writes with its releases, still has no row.
Lines: `core.rs` 8,948 to 8,968. `direct.rs` 21,549 to 21,565: the `Finds` arm and `map_find`'s signature outweigh the two deleted copies. Refusals: 0 lost / 0 gained. Manifest: untouched.
Licence, at the tip, rebased onto main `af4c7316`:
- `coredrive --ignored` under `VYRN_WASM_MANIFEST=check`: taken 20,931 to 20,938 of 21,172, as predicted (+5 for G2, +2 for G3). Carried end to end 1,649 to 1,652. 1 byte-identical, 167 run the same, 0 run apart. On `513bae71` the same two commits took 20,927 to 20,932 to 20,934. The only bodies that moved are the seven named above.
- `vyrn check` over the corpus roots with main's binary and the tip's: `diff -r` is empty, 495 roots, 419 accepted, 76 refused.
- `kernel` 27,061 / 0 / 0 on main and at the tip. `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed.
- `VYRN_LEAK_CHECK=1 vyrn run` of `assoctype`, `falliblegeneric`, `protocol`, `intkeys`, `mapdemo`, `mapkey` and `wirekey`: main's binary and the tip's print the same bytes, each exits 0, and no audit line appears.
- pins by `VYRN_PIN=write` in the G3 commit: `emitter-census` the mapping 11,938 to 11,960 and one block per builtin name 4,421 to 4,415; `emitter-reads` the core's rows 2,999 to 2,993 and both, for two questions 9,780 to 9,802; `surface` `Type::Bool` and `Type::Str`.
Findings:
- `impl_args` must choose the impl by its type key and not only by the mangled name. Every `Show` impl declares `show`, and `impl_method_name("Show", "Option", "show")` matched the `Int64` impl first, which left `T` unsolved. `protocol` `main` stayed refused until the key was checked.
- G1's first cut routed `Builder::place` through `at` and refused `examples/slots.vyrn` at emit: "`people[]` may not be stored into `people`". The store's move-out `let` shares the `let` arm of `Stmt::Let`. `coredrive` counts that program's bodies the same either way, so it does not see the refusal. The patch is `C:/wtappendtmp/m7/g1-at-projection.patch`.
- G1's rhs half is outside `0f0c8c66`: `prelude::signature("@at")` matches before `Callee::Projection`, so `w[0]` of a user container is a Builtin `@at` call and never reaches `inlined`. The bodies are `projection` `main` and `genref` `measure` (`strs[h].byteLength`).
Left: `projection` `main` and `genref` `measure`, blocked by `m7-window`'s `at` inline landing (the lead holds that list). `@remove` as a row, blocked by its entry releases, which the arm writes and `Spec` has no kind for.
