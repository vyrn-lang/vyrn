#### A borrow that a `modify` argument or a push-built root left without a place has a rule, stated once (2026-09-25, `m7-reads`)
RFC-0125, milestone M7.
Decision: #453, the lead's: the screen asks a name for its place. Two rules, one commit each. First, module state handed to `modify` is its place (`Arg::Place(Place::Global)`), as a window's place is, and the emitter passes a place argument of a declared callee as its address (`core_user_call`). Second, a write-back receiver (`out.push(x)`) changes no owner, so `core_alias` reads it as a modify of the root, which refuses a borrow only inside the borrow's extent.
Went: `name-no-place:Record:Read(Global)`, 9 bodies (`newCursor` and `takeCursor` in 4 programs, `tryplace` `ensureSlab`). Also `name-no-place:Record:Read(Elem)` beside a push-built root, 4 bodies (`knucleotide` `sortWith`, `htmltree` `i18n`, `vyxMergeImports` x2). Stayed: `SmallArray:Make`, 4 bodies, because no reader in the core walk builds a SmallArray. `Enum:Read` (8) was already gone on `c86af1ee`.
Lines: `direct.rs` 21,826 to 21,900; `core.rs` 9,203 to 9,210 (measured on `c86af1ee`). The change adds more than it deletes because `core_user_call` is a new reader of place arguments. Refusals: 0 lost / 0 gained. Manifest: 7 rows, listed below.
Licence:
- census instrument (`instr-census.patch`, uncommitted), on `c86af1ee` and at the tip: taken went from 24,250 to 24,263 of 24,510 walks. 13 bodies went from refused to taken, and none went from taken to refused.
- `coredrive --ignored`, manifest check: taken 21,026 on `c86af1ee`, 21,035 after commit 1 (+9), 21,036 after commit 2 (+1, `sortWith`), of 21,172. `i18n` and `vyxMergeImports` are generator bodies, outside this count. After the rebase onto `c2f59d6f`: 21,053 to 21,063. At the tip on `6ad6dfc2`: 21,063. 1 byte-identical, 167 run the same, 0 run apart.
- manifest rows, named per function with `VYRN_WASM_NAMES=1` and `wasm2wat`. Commit 1 moved `newCursor` and `takeCursor` in `membench`, `streamlazy`, `streamops` and `streamunfold`. `newCursor` builds the cell at frame offset 0 and the handle at 16, where the arm used 24 and 0. `takeCursor` keeps the scalars of `cells[h].src` in locals. Commit 2 moved three rows. `knucleotide` `sortWith` now comes from the core. `smallarray` `main` reads a heapless element of a pushed array by address: one 40-byte `memory.copy` fewer, and the frame shrank from 1,088 to 1,056. `closures2` `main` walks a `for` over a pushed `Array` of fn values from the rows: one 16-byte copy fewer, and the frame shrank from 464 to 448.
- `VYRN_LEAK_CHECK=1` on the 7 moved programs and `tryplace`, with main's binary and the tip's: stdout, stderr and exit code were equal, and no run exited 134. `closures2` traps with exit 1 on both, by design.
- shapes `module-state-handed-to-modify` (exit 124) and `an-element-of-a-pushed-array-read-into-a-call` (exit -25): the instrument shows every function of both taken. `out.push(out[0])` of a heapless record is still refused, and prints 23.
- shape `an-element-read-before-pushes-that-grow-its-array`, the lead's witness: `let e = out[0]` of a heapless record, then 64 pushes. `grow` is taken, and `e` is a 16-byte `memory.copy` into its own slot before the loop. It prints 7095 on the core walk, the arm, main's binary and the interpreter, under `VYRN_LEAK_CHECK=1` on wasmtime. With a `String` field the checker refuses the read of `e`: "`out` is written here while `e` still reads out of it".
- `vyrn check` over the corpus against `6ad6dfc2`'s tree: every shared root is byte-identical, and the two new shapes are accepted. kernel: 27,061 accepted / 0 refused / 0 unlowered. effects: 29,619 judged, 9,945 pure. residue: engine 173 clean / 0 leaking, route 173 / 0.
- `lowered`'s compared-answers floor: 4,223 answers on `c2f59d6f`, 3,989 after commit 1, 3,954 at the tip. The floor went from 4,000 to 3,000 in commit 1 (#465).
Time: 190 minutes: 60 work, 95 gates, 20 for two rebases, 15 waiting.
Findings:
- the previous agent's uncommitted patch (a global handed to `modify` as a place) was sound. It landed as written, less one redundant `call_keeps` reset.
- an owned element name that is itself a write-back receiver now reads as modified, so it gets no alias. No corpus body has that shape.
Left: `SmallArray:Make` (4 bodies), blocked by a SmallArray layout that the core walk does not build. `slots` `main` is blocked by `@copy` of a record, and `mapkey` `main` by `Read(Key)`.
