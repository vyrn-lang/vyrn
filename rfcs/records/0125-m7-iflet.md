#### A made layout a statement does not bind, and an aggregate return under a branch, are the per-statement walk's (2026-09-24, `m7-iflet`)
Decision: a layout a run makes and does not bind lands in a slot, and the run carries the temporary's release; a `return` of a layout under an `if`, an `if let`, a `match` or a loop writes through `dest` as a tail `return` does. The lead's rule (brief `m7-iflet`); the readers are mine.
The count, from a temporary instrument in `Fn_::core_run` over `emit-wat` of the 174 programs of `examples/` that emit, first / only / met, occurrences in bodies, on `7cbd2e29`: a made layout not the statement's binding 2,680 / 104 / 3,187 in 515 / 43 / 537; an aggregate return under a branch 565 / 0 / 565 in 164. After the first commit 0 and 565 / 1 / 565; after the second 0 and 0, and 0 and 0 again on the rebased tip.
Went: the made-layout clause, the tail-only return clause, the top-level-only release skip. Stayed: a run that releases a stream, and a result checked where it is returned, on the arm (below).
Lines: `direct.rs` 21,198 to 21,247 to 21,265 on `2cf48cbd` (+135 / -68): the release walk at any depth and the checked-return clause are new state, and no clause of the screen was deleted whole. `core.rs` +20: `rows_by_statement` gives a temporary's release to the run that binds it, a rule no pass stated. `coredrive.rs` 972 to 990. Refusals: 0 lost / 0 gained. Manifest: 21 rows on `2cf48cbd` (24 on `2e7a7733`), in the first commit.
Licence:
- the arm tally over the 415 roots (`VYRN_FORM_TALLY`), main `2e7a7733` / first commit / tip: `Stmt::IfLet` 139 / 138 / 127; `Stmt::If` 9,539 / 9,543 / 8,825; `Stmt::Return` 5,491 / 5,430 / 5,009; `Stmt::Let` 8,343 / 8,281 / 7,595; every form 180,470 / 178,058 / 166,077. The `try` constructor 6 on `7cbd2e29` and 6 at the tip, in `parseServe` (twice each in `clidemo` and `clifail`) and `main` of `branchtypes`.
- `coredrive --ignored` on `7cbd2e29`, main / first / second: taken 20,179 each; the core's rows `Stmt::Let` 2,641 / 2,690 / 2,690, `Stmt::Return` 609 / 623 / 621, `Stmt::If` 682 / 681 / 679; `Stmt::IfLet` on the arm 66 / 65 / 65. At the tip on `2cf48cbd`: taken 20,418 of 21,176, main's number, plus 74 judged; 1,647 carried end to end; 1 byte-identical, 167 run the same, 0 run apart; `Stmt::IfLet` 37 on the arm, 0 on the rows. `SHAPES` gains a made layout the statement does not bind and an aggregate return under a branch.
- the 24 moved rows: 29 functions, each `main` or a named reader (`parseServe`, `gqlArgList`, `graphqlHandle`, `mount`, `integrate`, `ranked`, `parseAtom`, `parseConcat`, `row`). Two sampled in `wat`: `nbody`'s `integrate` reads the call result in its slot at an offset, and `knucleotide`'s `ranked` builds the record in its slot and drops a `memory.copy`. All 24 run the same under main's binary and the tip's with `VYRN_LEAK_CHECK=1`, 0 leak lines.
- frames over `emit-wat` of `examples/`: 6 shrank (`container`, `generics`, `jchain`, `matchown`, `tryplace` `main`; `graphqlHandle`), by 32 to 48 bytes; none grew.
- `vyrn check` over the 415 roots, main's binary against the tip's: `diff -r` empty, 339 accepted and 76 refused.
- `kernel`: 175 programs, 27,066 accepted, 0 refused, 0 unlowered. `effects`: 29,624 judged, 0 unattributed. `typed`: 236,919 judged, 0 unjudged. `coretables` and the manifest check green.
- `cargo nextest run --release -p vyrn-cli`: 666 passed, 0 failed, 47 skipped. Both formatters clean; the release build has no warning. `emitter_census` on `2cf48cbd`: the mapping 11,634 to 11,699, shared machinery 2,713 to 2,715, `neither` 6,655 to 6,657, `both, for two questions` 9,792 to 9,857. RFC-0126: `Stream` 33 to 34, 1,430 to 1,431 mentions.
- not run: `residue --ignored`, `vyrn bench --check`, the bench corpus.
Findings:
- the second commit moved no manifest row, yet on `7cbd2e29` it moved 2 `Stmt::If` from the rows to the arm and 2 `Stmt::Return` out of the count. Cause not isolated; the modules are byte-identical.
- a run that releases a stream stays on the arm: the arm hands a stream to its callee, and the core states its release after the call.
Left: every if-let the two clauses held stops at the next clause. Over `examples/` on `2cf48cbd` (an instrument at each refusal of `core_run`, not committed), 40 if-lets: 13 at an enum binding (6 in `tryplace`), 8 at a name not a scalar (`mount` 4), 7 with no core (`map` and `filter` of the stream examples), 5 at the form's type condition (`tryplace`), 3 at a String binding, 2 at another binding, 2 at `core_arm_ty` (an element read, `namedplace` and `jsonplace`). Blocked by: a binding the arm keeps off the operand stack (enum, String), which the screen reads as a scalar only; and a body with no core. Over every form the first clause is a name not a scalar (5,585), then an Array binding (1,501) and no core (1,333).

The iflet track (`m7-iflet`) closed two clauses of `Fn_::core_run`: a layout a run makes and does not bind, and an aggregate `return` under a branch. The arm tally over the 415 roots fell from 180,470 occurrences to 166,077, and `Stmt::IfLet` from 139 to 127. No whole body moved: `coredrive` takes 20,418 bodies with the track and without it. The if-lets the clauses held stop at the next clause, most at a binding that is not a scalar.
