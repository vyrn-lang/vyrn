#### The logging facade is one row kind, and both walks emit it through one helper (2026-09-25, `m7-logger`)
RFC-0125, milestone M7.
Decision: the lead's brief (`m7-logger`). Mine: `logger` and the five levels are one row kind, `Spec::Logs`, not `Spec::Effect`, because a level takes two operands and a threshold. Both walks call `Fn_::logs`, which states the identity handle, the threshold test and the write once.
Went: the AST arms of `logger` and `@trace`/`@debug`/`@info`/`@warn`/`@error`, moved whole into `Fn_::logs`; the 6 bodies the census names behind `Let/Call/Builtin/logger` and `Do/Call/Builtin/@<level>` (`clifail` `refuse`, `logging` `main`, `vlog` `recordsFrom`, `cmdFilter`, `usage`, `main`), none of which met another clause. Stayed: no logger body.
Lines: on `6ad6dfc2`, `direct.rs` 21,895 to 21,938 and `core.rs` 9,302 to 9,314. The row kind costs its variant, six rows, a type arm, a readable arm, a `core_call` arm and `logs_arity`, the one home of the operand count that the AST guard and the readable arm both ask; the helper's signature is 16 lines of rustfmt. Refusals: 0 lost / 0 gained. Manifest: 3 rows. The `lowered` floor went from 4,000 to 3,900 answers compared, because the AST walk answers 3,985 once the core walk takes these bodies.
Licence:
- `coredrive --ignored`, fast command, `b640e67d` against the slice: taken 21,033 to 21,039 of 21,172; carried end to end 1,656 to 1,660; 74 judged only; 1 byte-identical, 167 run the same, 0 run apart. At the tip on `6ad6dfc2`: 21,059 of 21,172, carried 1,660, 0 run apart, 374 s.
- The census instrument (`C:/wtstorestmp/logger-wip.patch`) on `c86af1ee`: 260 refused walks. Builtin clauses: `@at` 20, `unboxStream` 11, `logger` 6, `fromArray` 6, `@tally` 6, `fromStep` 5, `boxStream` 4, `@error` 4, `@push` 2, `@warn` 2, and 1 each for `schemaOf`, `@tallyBytes`, `@swapRemove`, `@pop`, `@trace`, `@info` and `@debug`. Predicted +6, 0 apart; got +6, 0 apart.
- The moved rows, `wasm2wat` with `VYRN_WASM_NAMES=1`, `c2f59d6f`'s binary against the slice's. `logging` `main` 230 to 240 lines: the message's interpolation is built into locals before the call, so the logger is read at the call (`local.set`/`local.get` in place of `local.tee`). `clifail` `refuse` 259 to 265: the core walk's `for` reads each `Issue` field through the element's address, so the 12-byte element copy is gone and the frame falls from 48 to 32 bytes. `vlog` `recordsFrom` 314 to 342, `cmdFilter` 345 to 367, `main` 694 to 715, the same two kinds; `usage` unchanged in length. Five release functions, 170 to 176, are emitted in a different order, because the core walk reaches them in another first-use order.
- The shape `a-logger-and-a-level-below-the-threshold`: a `debug` below the default threshold and a `warn` above it, both interpolated. Both walks run the same.
- `VYRN_LEAK_CHECK=1 vyrn run`, `6ad6dfc2`'s binary and the tip's: `clifail` with its `.args` (15 lines, exit 2), `logging` (4 lines, exit 0), `vlog` with its `.stdin` (20 lines), and `vlog` with `--help`, `bogus`, `filter --level nope`, `count --file /nonexistent` and `count` over a malformed line (15 `WARN`/`ERROR` lines, exits 2, 2, 2, 3, 0). Same output byte for byte, no audit line.
- At the tip on `6ad6dfc2`: `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `kernel --ignored`: 27,061 accepted, 0 refused, 0 unlowered. `effects` 29,619 judged, 0 unattributed; `typed` 236,909 judged, 0 unjudged; `coretables` and the manifest check green. `cargo nextest run --release -p vyrn-cli`: 672 passed, 45 skipped. `cargo test --release -p vyrn-lower -p vyrn-codegen` green. Both formatters clean; the release build has no warning. Re-pinned: `emitter-census` the mapping 12,249 to 12,268, one block per builtin name 4,462 to 4,486; `emitter-reads` the core's rows 3,072 to 3,115, both, for two questions 10,059 to 10,059; `surface` `Type::Logger` 7 to 8.
- `scripts/check-corpus.sh`, `6ad6dfc2` against the tip: 507 shared roots byte-identical, the shape gained and accepted (431 accepted, 77 refused).
Time: about 200 minutes: 20 work, 140 gates, 20 rebases (main moved twice), 20 waiting on a shared machine at 100% CPU.
Findings:
- nextest strips tab characters from captured stderr, so the instrument separates its fields with `^^`.
- `coredrive` took 280 s to 520 s on the loaded machine; a run with its build inside `timeout 600` was killed, so the test binaries were built first and the tip's run used `timeout 1200`.
Left: nothing for the facade. The other builtin clauses belong to `m7-stream` (`unboxStream`, `fromArray`, `fromStep`, `boxStream`) and `m7-mapcall` (`@tally`, `@tallyBytes`); `@at`, `@push`, `@pop`, `@swapRemove` and `schemaOf` are unassigned.
