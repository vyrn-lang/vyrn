#### A `for` over a user container is its `Iterate` expansion on the core's path too (2026-09-25, `m7-letpay`)
RFC-0125, milestone M7.
Decision: the lead's. The loop has one expansion, `project::iterate_loop`'s, for the arm, the lowered walk and the core. The rule for `jchain` `main` (a borrow bound out of a join takes the join's place) is `m7-small`'s, and this track dropped its copy.
Count, with the screen instrument in `core_walkable` on the `m7-atrhs` tip: `container` `main` met only two `Let/Read` of `Elem(w, i)`, its two `for x in w`. Prediction: +1. Measured: +1 on `ea737630`, +2 on `4f8a57e6`, where `m7-copy` had left `slots` `main` with only its `for p in people`.
Went: the core's element read of a user container in `for`, which no reader took. Stayed: a `for` over a path or module state and a consuming `for` over a user container keep the element read, because the loop cannot hold a borrow of a path by name.
Lines: `core.rs` 9,471 to 9,588: `Builder::iterated`, `Builder::iterate` and `Builder::scaffold` state the expansion, and the `ForIn` arm keeps its `Array` path. Refusals: 0 lost / 0 gained over the corpus. Manifest: 2 rows, below.
Licence, at the tip on main `4f8a57e6`:
- coredrive with `VYRN_WASM_MANIFEST=check`: taken 21,094 on main to 21,096 at the tip: `container` `main` and `slots` `main` (21,092 to 21,093 on `ea737630`). Carried end to end 1,666 on both. 1 byte-identical, 167 run the same, 0 run apart.
- rows from `wasm2wat` with `VYRN_WASM_NAMES=1`, main's binary against the tip's, `main` alone moves in each: `container` 907 to 879 lines, 4 to 0 `memory.copy`; `slots` 1,728 to 1,739 lines, 22 to 14 copies, frame 720 to 432.
- the loops over `Slots` in the corpus are two: `slots.vyrn` `for p in people` (`Slots<Person>`, a heap element) and `slottable.vyrn`'s test `for x in wide`. `VYRN_LEAK_CHECK=1 vyrn run slots.vyrn` and `vyrn test slottable.vyrn`, main against the tip: stdout, stderr and exit code equal, no audit line. The test build's frames are equal.
- `vyrn check` over the corpus, main against the tip: the 524 shared roots are byte-identical, 445 accepted, 79 refused. The new shape is accepted.
- refusals the corpus does not reach: `w.data.push(x)` and `w = Window {..}` inside `for x in w` over a user container. Both are refused on main and at the tip at line 5 with the same sentence. The tip adds a second refusal whose "used again" line is 1, `nth`'s read of `w.start`, because the kernel refuses a write once per dead read. Main does the same for `let r = a.x; a.x = [3]` read twice. The lead accepted the second refusal: the first is main's, and the second follows main's rule of one refusal per dead read. `refusals.rs` witnesses both stores.
- kernel 27,061 / 0 / 0. residue: engine 173 clean / 0 leaking, route 173 / 0.
- `VYRN_LEAK_CHECK=1 vyrn run` of the 11 examples that name `Slots` and `container`, `jchain`, `jsonplace`, `projection`, main against the tip: equal, no audit line.
- pins: `forms` `Stmt::While` 16 to 17 and `Expr::Var` 120 to 122.
Time: 290 minutes: 60 work, 145 gates on a loaded machine, 4 rebases (main moved from `31a3e69a` to `4f8a57e6`), 85 measuring main at each base.
Findings:
- the first cut read the container by name each turn, and two refusals the corpus never reaches were lost: a store into the container inside its own loop. The scaffolding now reads a borrow taken before the loop.
- `m7-small`'s `5fd5f0be` is this track's dropped rule, byte for byte in `direct.rs`, and moves `jchain` to the module hash measured here as taken on top of `m7-atrhs`: `jchain` `main` is taken once it merges.
- `print("\{r.length}")` on line 6 is worded "used again here" at line 1 on main: a node inside an interpolation hole reports line 1, #471, not the kernel.
- coredrive's forms pin: `Stmt::IfLet` had already left on main `d62c4f6e` (#499 with #507 to #511 merged together; CI does not run coredrive, so main went red unseen, found by `m7-mapleak`), and this slice takes the last per-statement `Stmt::If` rows with container and slots `main`. The pin drops both; a sharded run skips it.
Left: a `for` over a path (`for x in a.w`) or over module state of a user container waits for a payer: 0 corpus bodies on 2026-09-25 (two `impl Iterate`, `Window` and `Slots<T>`, every loop over a local). The lead's decision: when a payer exists, the expansion binds the container to `@i.c` (`project::iterate_loop_build`), so all engines read one expansion and the core's loop borrow is that name's; it first needs a movecheck witness that `let @i.c = a.w` of a heap container is a borrow. A consuming `for` over a user container keeps the arm.
