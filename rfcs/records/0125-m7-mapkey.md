#### A key read of any value is the lookup the aggregate `let` binds (2026-09-25, `m7-mapkey`)
RFC-0125, milestone M7.
Decision: `Fn_::core_agg_call` admits a map key read whatever the value's type. The lead's brief (`m7-mapkey`) put a condition first, and it held: a key read skips no projection prologue. `Map` is a builtin container, so `project::lookup_in` answers `None` for it, and both walks write the lookup through one `Fn_::map_at`, which copies the payload into the `Option`'s slot: one word, two words inline, or a boxed copy. The `!Agg` condition came from `m7-key` (2026-09-22). Its record says the frame then held only scalar names; it states no rule about a layout value.
Went: the value-type condition (1 line). Stayed: nothing.
Lines: `direct.rs` 21,387 to 21,389 (rustfmt split the `matches!`). Refusals: 0 lost / 0 gained. Manifest: 1 row, below.
Licence:
- measure-walk instrument, applied uncommitted, `d5833c57` (`m7-unit`) against the rule: taken 20,695 to 20,696 of 21,176, and the only body that moved is `nestedsum` `main`. The census row "an enum `@borrow` read out of a map key" met 5 bodies before. Of those, `nestedsum` `main` is taken now. The 4 `fnvalstore` `fire*` bodies pass the names clause and stop at `Let/Call`, the call through the payload-bound `cb` (below). `fieldmut` `main`, outside the row, no longer meets the key read and stays refused for a `Map` literal name and its takes.
- a first cut refused a boxed payload (`Word::Boxed`) and lost `simdmem2` `main`, a `Map<String, F32x4>` whose vector payload is boxed. That body was taken, runs the same and is leak-clean on both walks, so the cut was a guess and was withdrawn.
- `coredrive --ignored` at the tip: taken 20,696 of 21,176, 1 byte-identical, 167 run the same, 0 run apart; the new shape at 0 break, 0 continue.
- the moved row, `nestedsum`, built with `VYRN_WASM_NAMES=1` by `d5833c57`'s binary and the tip's: `wasm2wat` differs in `$main` alone. Frame 320 to 160 bytes, `memory.copy` 27 to 21, 20 more `drop`s (the in-place build's pairs), and each `local.tee` became `local.set` and `local.get`. Not measured for time.
- the shape `a-map-key-read-of-a-layout-value`: `match m[k]` over an `Option<Int64>` value (boxed, since the payload is a sum), a two-word record (inline) and an `Array` (boxed). With the instrument on each screen, the three bodies are refused on `d5833c57` (`name-no-place:Enum:@borrow=Read(Key(..))`) and taken with the rule. Both binaries print 3353898 with the core walk on and off, under `VYRN_LEAK_CHECK=1`, with no audit line. `nestedsum` gives the same output under `VYRN_LEAK_CHECK=1` on both binaries.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `kernel` 27,066 / 0 / 0.
- `vyrn check` over the corpus roots, `e3e6023a`'s binary against the tip's: the 481 shared roots equal; 4 shapes gained, all accepted.
- pins by `VYRN_PIN=write`: `emitter-census` the mapping 11,808 to 11,810; `emitter-reads` both, for two questions 9,965 to 9,967.
Findings:
- a leak on main, in both walks: `let o = m[k]` of a value whose payload `map_at` boxes (a sum inside an `Option`, an `Array`) never frees the box. The core binds `o` as a borrow of `m[..]`, and the arm frees nothing either. `match m[k]` frees it, because the scrutinee is a temporary the plan releases. The `VYRN_LEAK_CHECK=1` witness: `Map<String, Array<Int64>>`, `let o = m[k]`, then `if let Some(xs) = o`, gives "free audit: 1 block(s), 24 bytes, never freed" on `e3e6023a`'s binary and the tip's, with the core walk on and off. Not fixed here.
Left: the 4 `fnvalstore` bodies, blocked by a core reader for a call through a `fn` value (`Callee::Value`): `core_rhs_readable` has no case for it, and `core_sig` answers `Callee::Fn` alone.
