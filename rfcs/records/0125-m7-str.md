#### `@str` reads a String temporary as any operand, and the drop row frees it (2026-09-23, rebased 2026-09-24, `m7-str`)
Decision: a String temporary that `@str` renders is a name with a slot and an extent, and the release row the driver placed after the render frees it. The render reader copies and frees nothing, so the screen's clause on a minted String name had no reason left. No node kind and no field. The lead's rule; the reading is mine, on the counts below.
The count, on `ad2cc03a`, from a temporary instrument in the clause (`Fn_::core_builtin_readable`) over `emit-wat` of the 174 programs of `examples/` that emit: with the clause refusing, it was reached in 91 program-body pairs, 35 distinct bodies; with it passing, it met 83 pairs and 140 temporaries. By what the temporary is, reached first (pairs) / met (temporaries): a String read out of a place (`Rhs::Read`) 51 / 47; a user call's result (`Callee::Fn`) 27 / 79; a nested `@str`, the value of `.toString()`, 12 / 13; a name's value (`Rhs::Val`) 1 / 1; a `+` or `@concat` 0 / 0. `emit-lowered` of a shape shows the rows: `let @t2 = builtin @str(read @t1)` then `drop @t1`.
Went: the clause, 11 lines. Stayed: `str_value`'s inline free of a String temporary, because the arm still emits `@str` in bodies the screen refuses for another clause.
Lines: `direct.rs` 20,945 to 20,937 (the clause -10, the splice fix +2). `core.rs` 8,565, untouched. `coredrive.rs` 868 to 876. `codequotes.rs` 277 to 300. Refusals: 0 lost / 0 gained. Manifest: 26 of 174 rows, written in the reader's commit.
Licence:
- `coredrive --ignored` on `b5e78d2c`: 168 programs, 21,146 bodies. Taken 20,193 to 20,250 of 21,176, plus 74 judged. Carried end to end 1,637. 1 byte-identical, 167 run the same, 0 run apart. `SHAPES` gains `@str` of a String temporary: a field read, a `+`, a call result and a rendered number.
- the moved rows in `wat`: 19 were named on `ad2cc03a` by three shapes (the slot per temporary, with the same calls and frees in the same order; a `for` head the rows carry, whose element is an address; a literal the rows intern where they read it). The 7 the new base adds (`domdemo`, `pagesdemo`, `protoplace`, `rpc`, `rpcsplit`, `scan`, `bin/server`) change 22 functions: 15 keep their calls in order, 5 of them moving only data addresses, and 7 build each hole's temporary before the concat that reads it, which moves a literal's push and a runtime call past each other.
- `vyrn check` over the 415 roots, main's binary against the tip's: `diff -r` empty, 339 accepted and 76 refused.
- `kernel --ignored`: 175 programs, 27,066 accepted, 0 refused, 0 unlowered. `effects`: 29,624 judged, 0 unattributed. `typed`: 236,919 stores judged, 0 unjudged. `coretables` and the manifest check green.
- `cargo nextest run --release -p vyrn-cli`: 666 passed, 0 failed, 47 skipped. Both formatters clean; the release build has no warning. `emitter_census`: the mapping 11,410 to 11,402 lines, `both, for two questions` 9,592 to 9,584. RFC-0126's surface table: `Str` in wasm 83 to 81, 1,427 to 1,425 mentions.
- on `ad2cc03a`, not rerun: `residue --ignored` engine 173 clean / 0 leaking, route 173 clean / 0 leaking; `VYRN_LEAK_CHECK=1 vyrn bench --check` 0 failed; the bench corpus, 17 programs, best of three interleaved, median ratio 1.000, no row outside its band, `vyrn bench --compare` at x1.50 no regression.
Findings:
- the screen's clause guarded a double free that could not happen: the core reader never passed the operand's expression, so it never freed inline, and the drop row was read since `m7-drop`.
- on the new base the slice took a body of `std/icons` whose `@codeSplice` read a field temporary carried on the stack, and `@codeSplice` writes its tag first: the generator's module failed to validate and `std_suite` and five `icons` tests went red. Main has the same defect for any field read spliced into a quote (a six-line generator fails on main's binary). `core_first_read` now carries nothing into `@codeSplice`; `codequotes` witnesses it.
Left: `str_value`'s inline free, blocked by every body the screen refuses for another clause that renders a String temporary (4 pairs over `examples/` on `ad2cc03a`, the other clause not instrumented).

The render track (`m7-str`) changed no gap. The emitter's screen refused `@str` of a String name the core minted, because the arm frees such a temporary where it copies it; the rows state that release as a drop row after the render, which the core walk reads since the drop track, and the render reader frees nothing. Over `examples/` the clause met 83 bodies, per program, and 140 temporaries: a String read out of a place, a user call's result, a nested `@str`, and never a concat, whose bodies stop earlier. `coredrive --ignored` reads 20,988 bodies the rows carry and 1,637 carried end to end, and the emitter takes 20,250 bodies where main took 20,193.
