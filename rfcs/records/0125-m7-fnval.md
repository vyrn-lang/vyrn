#### A specialization calls its target, a stored function name is its closure variant, and a stream handed to a call is the callee's (2026-09-24, rebased onto `m7-store` 2026-09-24, `m7-fnval`)
Decision: the lead's, "The gap tail, decided": function values keep both mechanisms and the core models both; and the lead's, that a `Stream` argument is consumed by the call. Mine: that rule stands where the core's `call` sets each position's capability and not in `movecheck::arg_verdict`, because the release came from the read position's own drain and the verdict only keys the emitter's argument drop; a stored function name is minted as a nullary variant is, owning nothing.
Went: the screen clause that stood a stream temporary's release down (`8b3606dd`, never merged); `ho_call`'s instance shell, into `ho_shell`, which both walks call; `core_instance`'s solution, into `solved_instance`; `build_fnval`'s two stores, into `fnval_into`. Came: `core::Target`, `Rhs::Call`'s `targets`, `core::specialize`, `Ctor::Closure`, `Fn_::core_ho`, `core_target`, `core_closure`. Stayed: the call through a stored value, the lazy force and a lambda as a target, which `Left` names.
Lines: `direct.rs` 20,944 to 20,944, 21,077 and 21,124; `core.rs` 8,565 to 8,576, 8,724 and 8,755; `coredrive.rs` 868 to 876, 888 and 895. Refusals: 0 lost / 0 gained. Manifest: 0, 10 and 2 rows.
Licence at the tip, rebased onto `m7-store` (`b3324a19`, on main `75384f3a`) on 2026-09-24, pins taken from store's and re-pinned per commit: `coredrive --ignored` taken 20,382 to 20,418 of 21,176, plus 74 judged; whole 20,988 to 21,003; carried end to end 1,637 to 1,647; 1 byte-identical, 167 run the same, 0 run apart; `SHAPES` 50 to 53. `direct.rs` 21,019 to 21,199, `core.rs` 8,565 to 8,755, `coredrive.rs` 924 to 951. Manifest 11 rows. `emitter_census` the mapping 11,484 to 11,635 lines, `both, for two questions` 9,666 to 9,793 with 288 to 290 rows; RFC-0126 1,427 to 1,430 mentions, RFC-0127 858 to 859. `vyrn check` over the 415 roots, main's binary against the tip's: `diff -r` empty, 339 accepted and 76 refused. `kernel` 27,066 accepted, 0 refused, 0 unlowered; `effects` 29,624 judged, 0 unattributed; `typed` 236,919 judged, 0 unjudged; `coretables` and the manifest check green. `cargo nextest run --release -p vyrn-cli` 666 passed, 0 failed, 47 skipped. Both formatters clean; the release build has no warning. Not rerun: `residue`, the leak check, the bench corpus.
Licence, on main `41ef02a0`, per commit:
- `coredrive --ignored`: taken 20,193 of 21,176, then 20,193, 20,220 and 20,229; whole 20,988 to 20,988, 20,994 and 21,003; carried end to end 1,637 to 1,637, 1,641 and 1,647; 0 run apart. `SHAPES` gains a stream a function hands back, a call through a `fn` parameter bound to a named function, and a function's name stored as a value; the first ran apart on the parent's core walk (23 against 123).
- the count, measured on `86aecc9e` before the last two merges: `VYRN_GAP_TALLY` over `emit-wat` of the 415 roots, and `m7-tail`'s screen instrument over the 174 programs of `examples/` that emit, second pass:

| | main | specialization | stored name |
|---|---|---|---|
| lines with a gap | 617 | 563 | 506 |
| `Call:Value:*` first / alone / met, 35 spellings | 119 / 119 / 153 | 119 / 119 / 153 | 121 / 120 / 153 |
| `Lambda` first / alone / met | 41 / 23 / 67 | 47 / 30 / 67 | 48 / 31 / 67 |
| `Opaque:Static` first / met | 141 / 224 | 64 / 146 | 0 / 80 |
| the screen, refused / no gap | 877 / 724 | 856 / 722 | 842 / 722 |

- the moved rows against the parent: `capturefn`, `fnvalarg`, `graphql` (the `gql*` specializations and their callers), `knucleotide`'s `ranked`, `membench`'s `streamCycles`, `pagesdemo`'s `data`, `rpc` and `rpcsplit`'s completion callers, `streamops`' `pipeline` and combinators, and `streamunfold` are taken whole at the specialization; `fnvalarg`'s `sideFn` builds its result in place, and `rest`'s handlers and `std/http`'s route builders store a named handler. Each prints and exits the same as the parent under `VYRN_LEAK_CHECK=1`.
- frames, on `86aecc9e`, `VYRN_FRAME_TRACE` over `examples/`, 23,250 functions: 0 grew; 32 and 7 shrank, 2,892,048 to 2,889,248 to 2,888,560 bytes.
- `vyrn check` over the 415 roots, main's binary against each commit's: byte-identical stdout, stderr and exit code, 339 accepted and 76 refused.
- `kernel --ignored` at each commit: 27,066 accepted, 0 refused, 0 unlowered. At the head: `effects` 29,624 judged, 0 differ; `typed` 236,919 judged, 0 unjudged; `coretables` green; `residue --release --ignored`, 363 s: engine 173 clean / 0 leaking, route 173 clean / 0 leaking; `VYRN_LEAK_CHECK=1 vyrn bench <f> --check` over the 17 bench programs: 78 ok, 0 failed.
- `cargo test --release -p vyrn-cli`: 665 passed, 0 failed, 47 ignored; `vyrn-lower` and `vyrn-codegen` 47 passed; `memory` 17 passed. Both formatters clean, no warning, the manifest check green. `emitter_census` re-pinned: the mapping 11,410 to 11,514 to 11,561 lines, `both, for two questions` 9,592 to 9,672 to 9,719 with 286 to 287 to 288 rows. RFC-0126 1,427 to 1,429 to 1,430 mentions; RFC-0127 858 to 859.
- the bench corpus at the head, 17 programs and 78 benches, main and head interleaved per program, three rounds alternated, best of three: median ratio 0.986, from 0.517 to 1.669, median noise band 1.234. The higher-order rows: `langbench`'s fn-value dispatch x0.984, `knucleotide` x1.015 and x0.928, `membench`'s stream rows x1.027, x1.092 and x1.182 (the last in a band of 1.243). One row sat past its band: `simdbench`'s `floor in Vyrn`, whose module is byte-identical. `vyrn bench --compare` at x1.50: 12 of 17 clean on the first run; `langbench`, `membench`, `revcomp` and `tryplace` were clean on the rerun, and `nbody` flagged x1.51 on a byte-identical module, where main's binary flagged `membench` against its own best of three.
Findings:
- the stream defect was wider than a function that hands its stream back: `sum(feed(3))` freed its buffer twice on the parent's core walk. A named stream was already the callee's.
- the `Call:Value` tally did not move at the specialization: the tally counts the declaration's frame, whose call stays `Callee::Value`.
- a call through a stored value read as one dispatcher call (built, held off this branch) makes `let f = b.body f()` cheaper than `print(b.body)` on a `lazy` field, and `lazyfield`'s byte-identity witness of "a `lazy T` field is `fn() -> T`" fails: the core states the force as a read of a String place, which no row calls.
Left: the lazy force as a read of the stored value and a `Value` call through it, with the forced field's release keyed by that read: the first commit of `m7-fnval-b`; a call through a stored value as one call to the signature's dispatcher, 11 bodies first of the 174 over seven names (`callN`, `runChain`, `runOps`, `httpFeed`, `httpInvoke`, `httpInvokeWhole`, `httpAsk`), with `core::gaps` naming only a `Value` row through a bound parameter, blocked by the lazy force; a lambda as a target, 4 bodies first (`apply` twice, `firstOf`, `applyTo`) and the `Lambda` tally (48 first, 67 met), and `ho_call` and `fn_binds` with it, blocked by the step before it; `gqlProjectEach` at a take of a field that is an enum, and `gqlPick` at a store into a name that is a record.

The function-value track (`m7-fnval`) took the RFC-0023 half for a named target and the stored name. A call row to a higher-order function names each target, and `core::specialize` states the instance per target, whose calls through the bound parameter are `Callee::Fn`; both walks share that instance. A function's name the checker types as a value is a make of its closure variant. A stream handed to a call is taken by it, which the parent's core walk closed a second time. Five of the eight bodies `m7-tail2` left at a call through a function value are taken; `gqlProjectEach` and `gqlPick` stop at a take of an enum field and a store into a record, and `map` was not a refused body. What stays is a call through a stored value, 11 bodies first, which waits on the lazy force, and a call through a parameter bound to a lambda, 4. On `86aecc9e` the screen refused 842 bodies where it refused 877, 722 with no gap where 724; the gap tally read 506 lines with a gap where 617, and `Opaque:Static` is no body's first gap. On `41ef02a0` `coredrive --ignored` reads 21,003 with no gap and 1,647 carried end to end, where 20,988 and 1,637, and the emitter takes 20,229 bodies where it took 20,193.
