#### A store into any place with an address lands by one rule, and a rebuild's result keeps its receiver's slot (2026-09-23, rebased 2026-09-24, `m7-store`)
Decision: the lead's: a store into a layout place is the core walk's, the row names the place and the value, the displaced value is released by the path `m7-box` states, and the value lands by `agg_landed`, the landing a `let` uses. The snapshot `Fn_::snap_at` keeps is freed by `Fn_::free_snap`, which calls `m7-box`'s `Fn_::emit_rel`. Mine: a map key store is `Fn_::map_set`, the insert the arm and a map literal make; a rebuild pairs with its store across the releases of its argument temporaries (`drops_ahead`); a rebuild that no store puts back hands its result the receiver's slot; a row keeps every slot it takes for another name, because a store's snapshot is such a slot and `std/regex`'s `parseConcat` ran apart when the row gave it back.
Went: the screen's refusals of a layout store into a name, of a String or layout store into a field and an element, of a map key store, and of a rebuild with no store after it, which an accumulator's append keeps. Stayed: module state, `m7-state`'s on main; a map key that is a layout, because the screen reads a key as one wasm local; an element store of a user container, resolved at the site by RFC-0120; a join name of type `Unit`; a push joined by an `if` expression, refused at the `let` that moves the join.
Lines, rebased onto main at `5b95a16b`: `direct.rs` 20,921 to 20,931, 20,951, 20,953, 20,952, 20,981, 20,991 and 21,003 over the seven code commits. `coredrive.rs` 796 to 844. `lowered.rs` 5 lines moved, its has-type floor 150 to 50. `core.rs` untouched. The emitter grew 82: the store arm over any place with an address, the key arm and the pairing. Refusals: 0 lost / 0 gained. Manifest: 0, 23, 4, 1, 7, 7 and 13 rows, each written in the commit that moved it; on `312f756b` the six store commits moved 5, 1, 0, 5, 7 and 1.
The count, from a temporary instrument in `Fn_::core_walkable` and `Fn_::core_readable` over `emit-wat` of the 174 programs of `examples/` that emit, second pass, every refusing clause recorded and passed, one line per body per program, `312f756b` against the head on that base, not re-run on `5b95a16b`: refused bodies 1,981 to 1,932. A row sums the clauses it names.

| store clause (first / only / met) | base | head |
|---|---|---|
| into a name that is an array, an enum or a record | 74 / 5 / 433 | 0 / 0 / 0 |
| into a field that is a String, an array or another layout | 20 / 14 / 58 | 0 / 0 / 0 |
| into an element of a builtin array, of any type | 2 / 0 / 11 | 0 / 0 / 0 |
| into a map key | 15 / 11 / 30 | 0 / 0 / 2 |
| a rebuild with no store after it | 15 / 1 / 69 | 0 / 0 / 0 |

Licence at the tip, rebased onto main at `75384f3a` on 2026-09-24, pins taken from main and re-pinned per commit: `coredrive --ignored` taken 20,250 to 20,382 of 21,176, plus 74 judged; carried end to end 1,637, unmoved; 1 byte-identical, 167 run the same, 0 run apart; `SHAPES` 44 to 50. `direct.rs` 20,937 to 21,019, `coredrive.rs` 876 to 924. Manifest 39 rows, each written in its commit. `emitter_census` the mapping 11,402 to 11,484 lines with 837 to 838 rows, `both, for two questions` 9,584 to 9,666 with 286 to 288; RFC-0126 1,425 to 1,427 mentions. `vyrn check` over the 415 roots, main's binary against the tip's: `diff -r` empty, 339 accepted and 76 refused. `kernel` 27,066 accepted, 0 refused, 0 unlowered; `effects` 29,624 judged, 0 unattributed; `typed` 236,919 judged, 0 unjudged; `coretables` and the manifest check green. `cargo nextest run --release -p vyrn-cli` 666 passed, 0 failed, 47 skipped, `fieldstore` and `memory` among them. Both formatters clean; the release build has no warning. Not rerun: `residue`, the leak check, the bench corpus.
Licence, on `5b95a16b`:
- `coredrive --ignored`, each commit built and run: taken 20,480 to 20,480, 20,555, 20,569, 20,570, 20,582, 20,592 and 20,604 of 21,512; whole 21,340 and carried end to end 1,648, unmoved; 1 byte-identical, 167 run the same, 0 run apart, throughout. On the arm `Stmt::Let` 2,650 to 2,294, `Stmt::Assign` 1,725 to 1,428, `Stmt::Drop` 430 to 246, `Stmt::While` 1,601 to 1,506, `Stmt::ForIn` 201 to 178. `SHAPES` 34 to 40; the six and the nested store's shape print the same under both walks with `VYRN_LEAK_CHECK=1` and report no block. On `312f756b` the same commits took 19,648 to 19,682.
- the moved rows, per function with `VYRN_WASM_NAMES` against the commit before, `memory.copy` counted. The name store, 23 rows: `std/jsonread`'s `parseArray` 13 to 9 and `parseObject` 17 to 12 in 16 programs, and 15 generated decoders one or two fewer each; `patchdemo`'s `diffKeyed` 25 to 18; `fannkuch`'s `foldCount` 4 to 3; `record`'s `main` shorter; `clearkeep`, `map`, `nbody`'s `integrate`, `spectralnorm`'s `spectralNorm` and `regexredux`'s `parseConcat`, `parseAlt` and `matchAt` land the value in the name's place with copies unmoved, 4 to 29 lines of `wat` longer each; 19 functions of `simdbench` compute a vector into a local before the address of the element it lands in. The field store, 4: `placeorder`'s `main` 22 to 4, `regexredux`'s `bracketClass` 4 to 2, `copy`'s `records`, and 7 functions of `std/http` in `rest`, which keep a String field's old pointer, store and free it. The element store, 1: `copy`'s `arrays` 12 to 7. The map key, 7: 11 map-building functions in 7 programs insert with `map_set`'s bytes, `regionescape`'s `main` 5 to 2 and `rest`'s `httpMatch` 7 to 3. The argument release, 7: `std/html`'s `escapeText` 10 to 6 and `escapeAttr` 6 to 4. The unstored rebuild, 13: `std/jsondec`'s `readDoc` 7 to 5 in 12 programs, and `map`'s `put`. The 32 moved programs print the same under main's binary and the head's with `VYRN_LEAK_CHECK=1`, and none reports a block.
- `vyrn check` over the 415 roots of `examples/`, `std/`, `site/` and `compiler/vyrn-cli/tests/`, main's binary against the head's: byte-identical stdout, stderr and exit code, 338 accepted and 77 refused.
- `kernel --ignored`: 175 programs, 27,416 accepted, 0 refused, 0 unlowered. `effects`: 29,988 judged, 0 differ. `typed`: 238,193 judged, 0 unjudged. `coretables` green. `fieldstore` 7 and `memory` 16 passed. `residue --ignored --release`, 505 s: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `VYRN_LEAK_CHECK=1 vyrn bench <f> --check` over the 17 bench programs: 78 ok, 0 failed.
- the bench corpus, 17 programs and 78 benches, main and head interleaved per program with the order alternated per round, three rounds, best of three: median ratio 1.000, from 0.662 to 1.089, median base band 1.148. `spectralnorm` x0.662, `nbody` x1.002, `fannkuch` x0.958, `membench`'s four array read rows x1.012 to x1.029. `vyrn bench --compare` at x1.50 against main's best: 16 of 17 clean; `simdbench` flagged `nearest in Vyrn` x1.67, was clean on a rerun and flagged five other rows on a third. Five more interleaved rounds of `simdbench` read a median of x0.997, from 0.859 to 1.016, in base bands up to 4.49.
- `cargo test --release -p vyrn-cli` as `--lib --bins` and four `--test` groups over the 85 targets: 662 passed, 0 failed, 47 ignored. `cargo test -p vyrn-lower -p vyrn-codegen`: 47 passed. Both formatters clean; the release build has no warning; the manifest check, `emitter_census`, `forms`, `surface`, `fieldstore` and `lowered` green at each commit. Re-pinned: `emitter_census` the mapping 11,276 to 11,358 lines with 837 to 838 rows, `both, for two questions` 9,813 to 9,895 with 280 to 282; RFC-0126's surface table, the wasm column of `Type::Map` 20 to 22, 1,429 to 1,431 mentions; `lowered`'s has-type count 350 to 141, 125, 125, 121, 79 and 79, so its floor went from 150 to 50 in the name-store commit.
Findings:
- the rebuild commit claimed a push joined by an `if` expression, and its shape's `two` stayed on the arm, refused at the `let` that moves the join. A fixup made the shape a push returned from an `if` arm, which the core takes, and the message says what moved.
- the store shape leaked 11 blocks on both walks at `312f756b`: the arm's snapshot freed an `Array<String>`'s buffer and not its elements. On main `free_snap` calls `emit_rel`, which frees both, and the shape reports no block.
- `Fn_::core_step` folds a field at offset 0 as `i32.const 0` and `i32.add`; `rest`'s String field stores show it. Not changed here, because it moves rows outside the slice.
Left: a map key that is a layout, blocked by the screen's key, one wasm local; an element store of a user container, blocked by RFC-0120's resolution at the site; the `Unit` join, blocked by the name clause; a push joined by an `if` expression, blocked by the move of an array (`Rhs::Val`), 73 first and 71 alone at the head of `312f756b`.

The store track (`m7-store`) changed no gap. A store into any place with an address is the core walk's: the address, the displaced value kept where the row releases it, the value landed by the copy a `let` uses, and the kept value freed; a map key store is `map_set`, the insert the arm and a map literal make. A rebuild pairs with its store across the releases of its argument temporaries, and a rebuild that no store puts back hands its result the receiver's slot. Over `examples/` a store into a name, a field, an element or a map key, or a rebuild with no store, was the first clause the screen refused in 126 bodies and is in 0. `coredrive --ignored` reads 21,340 with no gap and 1,648 carried end to end, both unmoved, and the emitter takes 20,604 bodies where it took 20,480.
