# A move that is no rename takes a slot and the bytes (2026-09-26, `m7-atrhs`)
RFC-0125, milestone M7.
Decision: the lead's (item 5 of the `main`-body naming). The shape, mine: `Fn_::core_copies` answers a move `let y = x` of an owned layout that owns heap, where neither name is a borrow, when `Fn_::core_renames` refuses it. `y` takes a slot and `x`'s bytes, as a take does. The kernel refuses a read of `x` before its next store, and the driver releases nothing a store finds moved.
Went: the `name-no-place` clause for such a move. Stayed: a move of a borrow, which has no bytes of its own to take.
Lines: `direct.rs` 22,693 to 22,700. Refusals: 0 lost / 0 gained. Manifest: untouched.
Licence, base `origin/m7-mains` `f7d384c4`:
- prediction: the shape `a-move-inside-a-loop-that-writes-the-moved-name-after-it` is taken, and coredrive does not move (no corpus body met the clause). Measured: both.
- the shape: 36 on both walks and on wasmtime, `VYRN_LEAK_CHECK=1`, no leak line, 0 arm forms. A witness with a store into the moved name while `y` lives, at the top and in a loop over Strings: 44 on both walks, no leak line.
- CLI suite 681/681 with `VYRN_PIN=write`: `emitter-census` and `emitter-reads` moved. `wasmhash`: no row moved.
Time: about 25 minutes: 10 reading, 5 the change, 10 gates.
