#### The worklist reads the releases the kernel places (2026-09-25, `m7-slotrel`)
RFC-0125, milestone M7.
Decision: the lead's brief `m7-slotrel`, and the lead's review, which replaced a one-round bound with a fixpoint. The lowering's worklist is the one list of the bodies the core builds, and `own.releases` is the one home of the placed releases. `augment` lowers again while the placement adds a generic declared release instance (`lib.rs` `dispatched` finds one among the rows). Each instance the new lowering adds is built, placed by `place_frames`, built again, and folded under its spelling. The measure is the set of instances not yet built: it is finite, and a round turns only after one that built at least one of them. No second list.
Went: nothing. Stayed: nothing on this row.
Lines: `core.rs` 8,948 to 8,992 on `af4c7316`, `lib.rs` 1,495 to 1,495 (`dispatched` is `pub(crate)`). Two shape files, 6 lines each; one `memory.rs` test, 36 lines. Refusals: 0 lost / 0 gained. Manifest: 10 rows, below.
Licence:
- the census row `no-core:no-instance`, with the census instrument applied and not committed, on `513bae71`: 13 `Owned__Slots__release` bodies, in `autorelease`, `copy`, `freelist`, `genref` (three), `linkedlist`, `membench`, `slots` (two), `slottable`, `tree` and `tryplace`. At the tip all 13 are taken. The prediction was 13 of 13.
- `coredrive --ignored` with `VYRN_WASM_MANIFEST=check`: taken 20,927 of 21,177 on `513bae71`, 20,940 at the tip on `513bae71` and on `a48a867e`, 20,935 of 21,172 on `6406139e`, and 20,944 of 21,172 on `af4c7316`. 1,649 carried end to end, 168 run the same, 0 run apart. Main alone was measured on `513bae71` only. Both shapes read 0 break, 0 continue.
- the 10 moved rows, per function from `wasm2wat` with `VYRN_WASM_NAMES=1` against main's binary: `Owned__Slots__release` alone, in each of the 10 programs. Its five field takes share one 24-byte slot at offset 128, where the arm gave each take a slot of its own. The frame is 160 bytes, down from 256. Each take also loses a staging local, and every body is 10 lines shorter.
- the shape `a-generic-declared-release-the-kernel-places`: `Owned__Pair__release` at two instances, 62 to 60 and 125 to 123 lines against main's binary, with the same output. The shape `a-generic-release-placed-inside-a-generic-release` takes the loop around twice: `lower: build` runs 4 times, not 2. `memory.rs` pins its run on both walks under `VYRN_LEAK_CHECK=1`: `6`, exit 0, empty stderr. On `6406139e`'s binary both walks print the audit line and exit 135.
- `VYRN_LEAK_CHECK=1` on the 11 examples that name `Slots`, with main's binary (`b144a6a9`) and the tip's: stdout, stderr and exit code are equal, and neither binary prints an audit line.
- `vyrn check` over the corpus, `b144a6a9` against the tip: the 493 shared roots are equal. The two shapes are the gained roots, and both are accepted.
- `residue --ignored`: engine 173 clean / 0 leaking, route 173 clean / 0 leaking, 0 failed. `kernel`: 175 programs, 27,061 accepted, 0 refused, 0 unlowered on `6406139e`, and 27,066 on `a48a867e` both before and after the change. The kernel suite lowers on its own, so neither count includes the new instances. The 5 fewer came with main's move to `6406139e`, and main alone was not run there.
- cost, `vyrn check`, placer phase, best of 5, interleaved, main's binary against the tip: `pagesdemo.vyrn` 330.2 to 335.7 ms, `site/export.vyrn` 1.077 to 1.052 s. Neither root places a generic release, so neither lowers again (`lower: build` runs 2 times in both). Both differences are inside the noise band, 330 to 440 ms and 1.05 to 2.10 s. On `membench.vyrn`, which lowers again, `checker::record` and `lower: build` run 3 times, not 2, and the placer makes 2 more core builds. Its placer phase, 15 interleaved rounds in one window, `6406139e`'s binary against the tip on `af4c7316`: best 41.1 to 37.8 ms, median 78.5 to 79.7 ms, maximum 156 and 153 ms. The earlier best of 7 (32.5 against 57.0 ms) was noise, and the second lowering is below this machine's resolution.
Findings:
- the placed rows reached `own.releases` only after `augment`'s one lowering, so `dispatched` read a plan that did not have them yet. The emitter reached each body through its ordinary call path, and the AST arm emitted it.
- main leaks where a generic release takes a field whose type has a generic declared release and never drops it. The shape's program prints `free audit: 1 block(s), 8 bytes, never freed` and exits 135 under `VYRN_LEAK_CHECK=1` on `b144a6a9`. The kernel had never built `Outer<T>`'s release, so it never placed `p`'s release. The tip frees it. The non-generic form of the same program does not leak on main.
Left: nothing.
