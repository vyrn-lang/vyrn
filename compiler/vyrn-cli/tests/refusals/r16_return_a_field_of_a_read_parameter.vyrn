// RFC-0089 rule 2 at the return: a field of a `read` parameter is a borrow.
type Doc = { title: String, id: Int64 }

fn titleOf(d: Doc) -> String {
    return d.title
}

fn main() -> Int64 {
    let d = Doc { title: "t", id: 1 }
    let s = titleOf(d)
    print(s.byteLength + d.id)
    return 0
}
