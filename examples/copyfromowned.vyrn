/// RFC-0115's second refusal: `copyFrom` overwrites the receiver's elements
/// by bytes, so its row carries `@Heapless` too (RFC-0125 §3 M6).
fn main() -> Int64 {
    let mut a: Array<String> = ["x"]
    let b: Array<String> = ["y"]
    a.copyFrom(b)
    return a.length
}
