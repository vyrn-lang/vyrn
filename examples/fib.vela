// Recursive Fibonacci — exercises functions, recursion, if, and comparison.
// `velac run examples/fib.vela` prints 55 and exits with code 55.

fn fib(n: Int) -> Int {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> Int {
    let result = fib(10)
    print(result)
    return result
}
